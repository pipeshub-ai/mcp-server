/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { smtpConfigurationGet } from "../../funcs/smtpConfigurationGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$smtpConfigurationGet: ToolDefinition = {
  name: "smtp-configuration-get",
  description: `Get SMTP configuration

Retrieve the current SMTP server configuration. Password is included in the response for admin users.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await smtpConfigurationGet(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
