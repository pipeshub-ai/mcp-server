/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oauthConfigurationUpdate } from "../../funcs/oauthConfigurationUpdate.js";
import { UpdateOAuthConfigRequest$zodSchema } from "../../models/updateoauthconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateOAuthConfigRequest$zodSchema,
};

export const tool$oauthConfigurationUpdate: ToolDefinition<typeof args> = {
  name: "oauth-configuration-update",
  description: `Update OAuth configuration

Update an OAuth configuration.<br><br>
<b>Admin Only:</b><br>
Only the creator or another admin can update.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oauthConfigurationUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
