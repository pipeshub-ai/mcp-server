/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentConversationsDelete } from "../../funcs/agentConversationsDelete.js";
import { DeleteAgentConversationRequest$zodSchema } from "../../models/deleteagentconversationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAgentConversationRequest$zodSchema,
};

export const tool$agentConversationsDelete: ToolDefinition<typeof args> = {
  name: "agent-conversations-delete",
  description: `Delete agent conversation

Delete a conversation with an agent.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentConversationsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
