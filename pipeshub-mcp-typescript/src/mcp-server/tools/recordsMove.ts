/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordsMove } from "../../funcs/recordsMove.js";
import { MoveRecordRequest$zodSchema } from "../../models/moverecordop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MoveRecordRequest$zodSchema,
};

export const tool$recordsMove: ToolDefinition<typeof args> = {
  name: "records-move",
  description: `Move a record to another folder

Move a record (file or folder) to a different parent folder within the same knowledge base.<br><br>
<b>Required Permission:</b> WRITER or higher<br><br>
<b>Moving to Root:</b><br>
Set <code>newParentId</code> to <code>null</code> to move the record to the root level of the knowledge base.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await recordsMove(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
