/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { platformSettingsUpdate } from "../../funcs/platformSettingsUpdate.js";
import { SetPlatformSettingsRequest$zodSchema } from "../../models/setplatformsettingsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetPlatformSettingsRequest$zodSchema,
};

export const tool$platformSettingsUpdate: ToolDefinition<typeof args> = {
  name: "platform-settings-update",
  description: `Update platform settings

Configure platform-wide settings including file upload limits and feature flags.

**File Upload Limits:**
- Default: 30MB (31457280 bytes)
- Maximum: 1GB (1073741824 bytes)

**Available Feature Flags:**
- ENABLE_BETA_CONNECTORS: Enable beta connector integrations (default: false)
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await platformSettingsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
