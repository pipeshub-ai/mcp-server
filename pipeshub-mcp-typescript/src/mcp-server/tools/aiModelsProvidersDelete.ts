/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiModelsProvidersDelete } from "../../funcs/aiModelsProvidersDelete.js";
import { DeleteAIModelProviderRequest$zodSchema } from "../../models/deleteaimodelproviderop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAIModelProviderRequest$zodSchema,
};

export const tool$aiModelsProvidersDelete: ToolDefinition<typeof args> = {
  name: "ai-models-providers-delete",
  description: `Delete AI model provider

Remove an AI model provider configuration. Cannot delete the default model if it's the only one.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await aiModelsProvidersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
