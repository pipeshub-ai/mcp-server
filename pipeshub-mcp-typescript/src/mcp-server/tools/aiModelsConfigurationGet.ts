/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiModelsConfigurationGet } from "../../funcs/aiModelsConfigurationGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$aiModelsConfigurationGet: ToolDefinition = {
  name: "ai-models-configuration-get",
  description: `Get all AI models configuration

Retrieve all configured AI model providers grouped by type.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await aiModelsConfigurationGet(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
