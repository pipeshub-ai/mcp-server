/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBasesGetChildNodes } from "../../funcs/knowledgeBasesGetChildNodes.js";
import { GetKnowledgeHubChildNodesRequest$zodSchema } from "../../models/getknowledgehubchildnodesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetKnowledgeHubChildNodesRequest$zodSchema,
};

export const tool$knowledgeBasesGetChildNodes: ToolDefinition<typeof args> = {
  name: "knowledge-bases-get-child-nodes",
  description: `Get knowledge hub child nodes

Retrieve child nodes under a specific parent in the knowledge hub tree.<br><br>
<b>Navigation:</b><br>
Use this to drill down into KBs, folders, and connector hierarchies.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await knowledgeBasesGetChildNodes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
