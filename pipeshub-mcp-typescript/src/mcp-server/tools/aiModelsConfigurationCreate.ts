/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiModelsConfigurationCreate } from "../../funcs/aiModelsConfigurationCreate.js";
import { AIModelsConfigInput$zodSchema } from "../../models/aimodelsconfiginput.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AIModelsConfigInput$zodSchema,
};

export const tool$aiModelsConfigurationCreate: ToolDefinition<typeof args> = {
  name: "ai-models-configuration-create",
  description: `Bulk create AI models configuration

Configure multiple AI model providers at once. Performs health checks on each model before saving. Use this for initial setup - for individual model management, use /ai-models/providers endpoints.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await aiModelsConfigurationCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
