/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorFiltersSave } from "../../funcs/connectorFiltersSave.js";
import { SaveConnectorFiltersRequestRequest$zodSchema } from "../../models/saveconnectorfiltersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SaveConnectorFiltersRequestRequest$zodSchema,
};

export const tool$connectorFiltersSave: ToolDefinition<typeof args> = {
  name: "connector-filters-save",
  description: `Save filter selections

Save the user's filter selections for a connector.<br><br>
<b>Overview:</b><br>
After viewing filter options, use this endpoint to save the
selected values. These determine what data will be synced.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorFiltersSave(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
