/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiModelsProvidersAddProvider } from "../../funcs/aiModelsProvidersAddProvider.js";
import { AddAIModelProviderRequest$zodSchema } from "../../models/addaimodelproviderrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddAIModelProviderRequest$zodSchema,
};

export const tool$aiModelsProvidersAddProvider: ToolDefinition<typeof args> = {
  name: "ai-models-providers-add-provider",
  description: `Add new AI model provider

Add a new AI model provider configuration. Performs a health check before saving to verify connectivity. Supported providers: openai, anthropic, azure-openai, aws-bedrock, google-vertex, ollama, huggingface.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await aiModelsProvidersAddProvider(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
