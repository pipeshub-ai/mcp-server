/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorGetStats } from "../../funcs/connectorGetStats.js";
import { GetConnectorStatsRequest$zodSchema } from "../../models/getconnectorstatsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetConnectorStatsRequest$zodSchema,
};

export const tool$connectorGetStats: ToolDefinition<typeof args> = {
  name: "connector-get-stats",
  description: `Get connector statistics

Retrieve statistics for a specific connector including record counts, indexing status breakdown, and sync information.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorGetStats(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
