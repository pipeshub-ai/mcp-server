/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetTeams } from "../../funcs/usersGetTeams.js";
import { GetCurrentUserTeamsRequest$zodSchema } from "../../models/getcurrentuserteamsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCurrentUserTeamsRequest$zodSchema.optional(),
};

export const tool$usersGetTeams: ToolDefinition<typeof args> = {
  name: "users-get-teams",
  description: `Get current user's teams

Get teams that the current user belongs to`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersGetTeams(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
