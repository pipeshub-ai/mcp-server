/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publicUrlsSet } from "../../funcs/publicUrlsSet.js";
import { URLConfig$zodSchema } from "../../models/urlconfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: URLConfig$zodSchema,
};

export const tool$publicUrlsSet: ToolDefinition<typeof args> = {
  name: "public-urls-set",
  description: `Set connector public URL

Configure the public URL for connector OAuth callbacks.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await publicUrlsSet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
