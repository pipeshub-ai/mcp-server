/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorOauthConfigurationGetGoogleWorkspace } from "../../funcs/connectorOauthConfigurationGetGoogleWorkspace.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$connectorOauthConfigurationGetGoogleWorkspace:
  ToolDefinition = {
    name: "connector-oauth-configuration-get-google-workspace",
    description: `Get Google Workspace OAuth configuration

Get Google Workspace OAuth configuration.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    tool: async (client, ctx) => {
      const [result] = await connectorOauthConfigurationGetGoogleWorkspace(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
