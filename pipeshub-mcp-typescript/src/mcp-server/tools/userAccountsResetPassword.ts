/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userAccountsResetPassword } from "../../funcs/userAccountsResetPassword.js";
import { PasswordResetRequest$zodSchema } from "../../models/passwordresetrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PasswordResetRequest$zodSchema,
};

export const tool$userAccountsResetPassword: ToolDefinition<typeof args> = {
  name: "user-accounts-reset-password",
  description: `Reset password (authenticated user)

Reset password for an authenticated user. Requires the current password for verification.
<br><br>
<b>Password Requirements:</b><br>
- Minimum 8 characters<br>
- At least 1 uppercase letter (A-Z)<br>
- At least 1 lowercase letter (a-z)<br>
- At least 1 number (0-9)<br>
- At least 1 special character (#?!@$%^&*-)
<br><br>
<b>Security Notes:</b><br>
- A new access token is returned (old tokens are invalidated)<br>
- CAPTCHA may be required if enabled (pass <code>cf-turnstile-response</code>)
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await userAccountsResetPassword(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
