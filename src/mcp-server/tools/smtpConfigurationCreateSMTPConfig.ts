/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { smtpConfigurationCreateSMTPConfig } from "../../funcs/smtpConfigurationCreateSMTPConfig.js";
import { SMTPConfig$zodSchema } from "../../models/smtpconfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SMTPConfig$zodSchema,
};

export const tool$smtpConfigurationCreateSMTPConfig: ToolDefinition<
  typeof args
> = {
  name: "SMTP-configuration-create-SMTP-config",
  description: `Create or update SMTP configuration

Configure SMTP email server for sending system emails including user invitations, notifications, and password resets.

Common SMTP providers and their settings:
- Gmail: host=smtp.gmail.com, port=587 (requires App Password)
- SendGrid: host=smtp.sendgrid.net, port=587
- Amazon SES: host=email-smtp.{region}.amazonaws.com, port=587
- Microsoft 365: host=smtp.office365.com, port=587

Configuration is encrypted before storage.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await smtpConfigurationCreateSMTPConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
