/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentTemplatesUpdateAgentTemplate } from "../../funcs/agentTemplatesUpdateAgentTemplate.js";
import { UpdateAgentTemplateRequest$zodSchema } from "../../models/updateagenttemplateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateAgentTemplateRequest$zodSchema,
};

export const tool$agentTemplatesUpdateAgentTemplate: ToolDefinition<
  typeof args
> = {
  name: "agent-templates-update-agent-template",
  description: `Update agent template

Update an existing agent template.<br><br>
<b>Permissions:</b><br>
Only the template creator can update it.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentTemplatesUpdateAgentTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
