/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetOAuthGetInstanceOAuthAuthorizationUrl } from "../../funcs/toolsetOAuthGetInstanceOAuthAuthorizationUrl.js";
import { GetInstanceOAuthAuthorizationUrlRequest$zodSchema } from "../../models/getinstanceoauthauthorizationurlop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetInstanceOAuthAuthorizationUrlRequest$zodSchema,
};

export const tool$toolsetOAuthGetInstanceOAuthAuthorizationUrl: ToolDefinition<
  typeof args
> = {
  name: "toolset-O-auth-get-instance-O-auth-authorization-url",
  description: `Get OAuth authorization URL for instance`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetOAuthGetInstanceOAuthAuthorizationUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
