/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchSearchHistory } from "../../funcs/semanticSearchSearchHistory.js";
import { SearchHistoryRequest$zodSchema } from "../../models/searchhistoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SearchHistoryRequest$zodSchema.optional(),
};

export const tool$semanticSearchSearchHistory: ToolDefinition<typeof args> = {
  name: "semantic-search-search-history",
  description: `Get search history

Retrieve the authenticated user's persisted search history.

Returns searches the user owns along with searches shared with them,
scoped to the caller's organization. Archived and deleted entries are
excluded. Citation references on this endpoint are returned as raw
identifier strings; use \`GET /search/{searchId}\` to fetch a single
search with its citations fully expanded.

Pagination defaults to \`page=1, limit=20\` (maximum \`limit\` is 100).
Results are sorted by most recent activity by default.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchSearchHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
