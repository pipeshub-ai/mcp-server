/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetAgentConversationById } from "../../funcs/agentsGetAgentConversationById.js";
import { GetAgentConversationByIdRequest$zodSchema } from "../../models/getagentconversationbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentConversationByIdRequest$zodSchema,
};

export const tool$agentsGetAgentConversationById: ToolDefinition<typeof args> =
  {
    name: "agents-get-agent-conversation-by-id",
    description: `Get agent conversation by ID

Returns the conversation with paginated/sorted messages and filter metadata.

**Message Pagination:**

Messages are paginated newest-first: \`page=1\` returns the most recent
batch. Increment \`page\` to load older batches (used by the infinite-scroll
"load older messages" feature).

- \`page\`: Page number (default: 1)
- \`limit\`: Messages per page (default: 20, max: 100)
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await agentsGetAgentConversationById(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
