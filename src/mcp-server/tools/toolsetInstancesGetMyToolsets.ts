/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetInstancesGetMyToolsets } from "../../funcs/toolsetInstancesGetMyToolsets.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$toolsetInstancesGetMyToolsets: ToolDefinition = {
  name: "toolset-instances-get-my-toolsets",
  description: `List my toolsets with auth status

Returns organization toolset instances merged with current user's authentication status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await toolsetInstancesGetMyToolsets(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
