/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentManagementUploadDocument } from "../../funcs/documentManagementUploadDocument.js";
import { UploadDocumentRequest$zodSchema } from "../../models/uploaddocumentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadDocumentRequest$zodSchema,
};

export const tool$documentManagementUploadDocument: ToolDefinition<
  typeof args
> = {
  name: "document-management-upload-document",
  description: `Upload document

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Upload a new document to the storage system.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await documentManagementUploadDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
