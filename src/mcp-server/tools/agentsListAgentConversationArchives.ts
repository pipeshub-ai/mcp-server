/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsListAgentConversationArchives } from "../../funcs/agentsListAgentConversationArchives.js";
import { ListAgentConversationArchivesRequest$zodSchema } from "../../models/listagentconversationarchivesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAgentConversationArchivesRequest$zodSchema,
};

export const tool$agentsListAgentConversationArchives: ToolDefinition<
  typeof args
> = {
  name: "agents-list-agent-conversation-archives",
  description: `List archived conversations for an agent

Paginated list of archived conversations for the given agent key.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsListAgentConversationArchives(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
