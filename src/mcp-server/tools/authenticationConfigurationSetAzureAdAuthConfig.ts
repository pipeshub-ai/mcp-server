/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authenticationConfigurationSetAzureAdAuthConfig } from "../../funcs/authenticationConfigurationSetAzureAdAuthConfig.js";
import { AzureAdAuthConfig$zodSchema } from "../../models/azureadauthconfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AzureAdAuthConfig$zodSchema,
};

export const tool$authenticationConfigurationSetAzureAdAuthConfig:
  ToolDefinition<typeof args> = {
    name: "authentication-configuration-set-azure-ad-auth-a00",
    description: `Configure Azure AD authentication

Set up Azure Active Directory as an authentication provider for user login.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await authenticationConfigurationSetAzureAdAuthConfig(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
