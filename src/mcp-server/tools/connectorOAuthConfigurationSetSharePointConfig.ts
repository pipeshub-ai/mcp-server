/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorOAuthConfigurationSetSharePointConfig } from "../../funcs/connectorOAuthConfigurationSetSharePointConfig.js";
import { SharePointConfig$zodSchema } from "../../models/sharepointconfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SharePointConfig$zodSchema,
};

export const tool$connectorOAuthConfigurationSetSharePointConfig:
  ToolDefinition<typeof args> = {
    name: "connector-O-auth-configuration-set-share-point-cf2",
    description: `Configure SharePoint connector

Set up Microsoft credentials for SharePoint connector.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await connectorOAuthConfigurationSetSharePointConfig(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
