/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsUpdateAgentConversationTitle } from "../../funcs/agentsUpdateAgentConversationTitle.js";
import { UpdateAgentConversationTitleRequest$zodSchema } from "../../models/updateagentconversationtitleop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateAgentConversationTitleRequest$zodSchema,
};

export const tool$agentsUpdateAgentConversationTitle: ToolDefinition<
  typeof args
> = {
  name: "agents-update-agent-conversation-title",
  description: `Update agent conversation title

Updates the display title for an agent conversation owned by the caller.

The controller looks up the conversation by \`_id\`, \`orgId\`, \`userId\`,
\`agentKey\`, and \`isDeleted: false\`.

The request body uses the shared title validator (\`1..200\` chars), and
the controller trims the incoming title before saving it. A whitespace-only
title can therefore still return HTTP 400 even if the raw string is
non-empty.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsUpdateAgentConversationTitle(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
