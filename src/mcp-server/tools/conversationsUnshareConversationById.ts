/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsUnshareConversationById } from "../../funcs/conversationsUnshareConversationById.js";
import { UnshareConversationByIdRequest$zodSchema } from "../../models/unshareconversationbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnshareConversationByIdRequest$zodSchema,
};

export const tool$conversationsUnshareConversationById: ToolDefinition<
  typeof args
> = {
  name: "conversations-unshare-conversation-by-id",
  description: `Unshare a conversation

Revoke sharing for a conversation, making it private again.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await conversationsUnshareConversationById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
