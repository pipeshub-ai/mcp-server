/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorConfigurationsGetGoogleWorkspaceCredentials } from "../../funcs/connectorConfigurationsGetGoogleWorkspaceCredentials.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$connectorConfigurationsGetGoogleWorkspaceCredentials:
  ToolDefinition = {
    name: "connector-configurations-get-google-workspace-abc",
    description: `Get Google Workspace credentials status

Check if Google Workspace credentials are configured.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    tool: async (client, ctx) => {
      const [result] =
        await connectorConfigurationsGetGoogleWorkspaceCredentials(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
