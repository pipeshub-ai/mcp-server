/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsUnarchiveConversation } from "../../funcs/conversationsUnarchiveConversation.js";
import { UnarchiveConversationRequest$zodSchema } from "../../models/unarchiveconversationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnarchiveConversationRequest$zodSchema,
};

export const tool$conversationsUnarchiveConversation: ToolDefinition<
  typeof args
> = {
  name: "conversations-unarchive-conversation",
  description: `Unarchive conversation

Restore an archived conversation.

- Path params: \`conversationId\`
- Query params: none
- Body: none
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await conversationsUnarchiveConversation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
