/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchUnshareSearch } from "../../funcs/semanticSearchUnshareSearch.js";
import { UnshareSearchRequest$zodSchema } from "../../models/unsharesearchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnshareSearchRequest$zodSchema,
};

export const tool$semanticSearchUnshareSearch: ToolDefinition<typeof args> = {
  name: "semantic-search-unshare-search",
  description: `Unshare a search

Revoke sharing for a specific search result, making it private again.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchUnshareSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
