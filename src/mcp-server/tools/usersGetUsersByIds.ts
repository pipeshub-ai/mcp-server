/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetUsersByIds } from "../../funcs/usersGetUsersByIds.js";
import { GetUsersByIdsRequest$zodSchema } from "../../models/getusersbyidsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUsersByIdsRequest$zodSchema,
};

export const tool$usersGetUsersByIds: ToolDefinition<typeof args> = {
  name: "users-get-users-by-ids",
  description: `Get users by IDs

Retrieve multiple users by their IDs in a single request.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersGetUsersByIds(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
