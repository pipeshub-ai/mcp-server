/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsActivateOAuthApp } from "../../funcs/oAuthAppsActivateOAuthApp.js";
import { ActivateOAuthAppRequest$zodSchema } from "../../models/activateoauthappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ActivateOAuthAppRequest$zodSchema,
};

export const tool$oAuthAppsActivateOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-activate-O-auth-app",
  description: `Activate suspended OAuth app

Moves a suspended OAuth app back to \`status: "active"\`, restoring its ability to authenticate and obtain new tokens via \`POST /oauth2/token\`.

A revoked app cannot be reactivated (returns \`400\`); the only path back is to register a new app. Activating an app that is already active also returns \`400\`.

Creator-only.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsActivateOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
