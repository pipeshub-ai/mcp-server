/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentManagementDocumentDiffChecker } from "../../funcs/documentManagementDocumentDiffChecker.js";
import { DocumentDiffCheckerRequest$zodSchema } from "../../models/documentdiffcheckerop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DocumentDiffCheckerRequest$zodSchema,
};

export const tool$documentManagementDocumentDiffChecker: ToolDefinition<
  typeof args
> = {
  name: "document-management-document-diff-checker",
  description: `Check if document is modified

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Check if a document has been modified since its last saved version.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await documentManagementDocumentDiffChecker(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
