/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { foldersGetFolderChildren } from "../../funcs/foldersGetFolderChildren.js";
import { GetFolderChildrenRequest$zodSchema } from "../../models/getfolderchildrenop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFolderChildrenRequest$zodSchema,
};

export const tool$foldersGetFolderChildren: ToolDefinition<typeof args> = {
  name: "folders-get-folder-children",
  description: `Get folder children (alias for folder contents)

Retrieve the children (subfolders and records) of a folder.<br><br>
<b>Overview:</b><br>
This is an alias endpoint for <code>/knowledgeBase/{kbId}/folder/{folderId}</code>. Returns paginated list of records within the folder, with same filtering options as KB-level record listing.<br><br>
<b>Navigation:</b><br>
Use this endpoint to browse folder hierarchies. Response includes folder metadata and child items.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await foldersGetFolderChildren(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
