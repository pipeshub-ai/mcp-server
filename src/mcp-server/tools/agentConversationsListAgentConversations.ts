/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentConversationsListAgentConversations } from "../../funcs/agentConversationsListAgentConversations.js";
import { ListAgentConversationsRequest$zodSchema } from "../../models/listagentconversationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAgentConversationsRequest$zodSchema,
};

export const tool$agentConversationsListAgentConversations: ToolDefinition<
  typeof args
> = {
  name: "agent-conversations-list-agent-conversations",
  description: `List agent conversations

Get all conversations with a specific agent.<br><br>
<b>Overview:</b><br>
Returns conversations the user has had with this particular agent.
Agent conversations maintain the agent's context and capabilities.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentConversationsListAgentConversations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
