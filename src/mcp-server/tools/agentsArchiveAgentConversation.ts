/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsArchiveAgentConversation } from "../../funcs/agentsArchiveAgentConversation.js";
import { ArchiveAgentConversationRequest$zodSchema } from "../../models/archiveagentconversationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ArchiveAgentConversationRequest$zodSchema,
};

export const tool$agentsArchiveAgentConversation: ToolDefinition<typeof args> =
  {
    name: "agents-archive-agent-conversation",
    description: `Archive an agent conversation

Marks the conversation as archived for the authenticated owner.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await agentsArchiveAgentConversation(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
