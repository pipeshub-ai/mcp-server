/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsAddUsersToTeam } from "../../funcs/teamsAddUsersToTeam.js";
import { AddUsersToTeamRequest$zodSchema } from "../../models/adduserstoteamop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AddUsersToTeamRequest$zodSchema,
};

export const tool$teamsAddUsersToTeam: ToolDefinition<typeof args> = {
  name: "teams-add-users-to-team",
  description: `Add users to team

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Add one or more users to a team.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await teamsAddUsersToTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
