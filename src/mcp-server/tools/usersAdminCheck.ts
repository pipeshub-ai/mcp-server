/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersAdminCheck } from "../../funcs/usersAdminCheck.js";
import { AdminCheckRequest$zodSchema } from "../../models/admincheckop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AdminCheckRequest$zodSchema,
};

export const tool$usersAdminCheck: ToolDefinition<typeof args> = {
  name: "users-admin-check",
  description: `Check if user is admin

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Check whether the specified user has admin privileges. Returns 200 OK if the user is an admin.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersAdminCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
