/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetCurrentOrganization } from "../../funcs/organizationsGetCurrentOrganization.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetCurrentOrganization: ToolDefinition = {
  name: "organizations-get-current-organization",
  description: `Get current organization

Retrieve details about the authenticated user's organization.

**Overview:**

This endpoint returns the organization document for the current user's org, including profile data and configuration.

**Response Includes:**

- Organization profile (registeredName, shortName, contactEmail, domain)
- Account type
- Onboarding status
- Permanent address
- Creation and modification timestamps

**Use Cases:**

- Organization profile pages
- Settings and configuration screens
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await organizationsGetCurrentOrganization(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
