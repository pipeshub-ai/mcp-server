/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBasesUpdate } from "../../funcs/knowledgeBasesUpdate.js";
import { UpdateKnowledgeBaseRequest$zodSchema } from "../../models/updateknowledgebaseop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateKnowledgeBaseRequest$zodSchema,
};

export const tool$knowledgeBasesUpdate: ToolDefinition<typeof args> = {
  name: "knowledge-bases-update",
  description: `Update knowledge base

Update a knowledge base's name.<br><br>
<b>Required Permission:</b> OWNER or ORGANIZER<br><br>
<b>Validation:</b><br>
<ul>
<li>Name must be 1-255 characters</li>
<li>XSS protection applied to input</li>
</ul>
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await knowledgeBasesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
