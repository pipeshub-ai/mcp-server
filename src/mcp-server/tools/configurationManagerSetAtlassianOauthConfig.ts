/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerSetAtlassianOauthConfig } from "../../funcs/configurationManagerSetAtlassianOauthConfig.js";
import { SetAtlassianOauthConfigRequest$zodSchema } from "../../models/setatlassianoauthconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetAtlassianOauthConfigRequest$zodSchema,
};

export const tool$configurationManagerSetAtlassianOauthConfig: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-set-atlassian-oauth-config",
  description: `Set Atlassian OAuth config

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Configure Atlassian OAuth credentials for the organization.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerSetAtlassianOauthConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
