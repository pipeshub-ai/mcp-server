/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetAllUsersWithGroups } from "../../funcs/usersGetAllUsersWithGroups.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$usersGetAllUsersWithGroups: ToolDefinition = {
  name: "users-get-all-users-with-groups",
  description: `Get all users with groups

Retrieve all users in the organization along with their group memberships.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await usersGetAllUsersWithGroups(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
