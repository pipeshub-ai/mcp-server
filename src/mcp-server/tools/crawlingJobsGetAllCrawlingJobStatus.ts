/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crawlingJobsGetAllCrawlingJobStatus } from "../../funcs/crawlingJobsGetAllCrawlingJobStatus.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$crawlingJobsGetAllCrawlingJobStatus: ToolDefinition = {
  name: "crawling-jobs-get-all-crawling-job-status",
  description: `Get all crawling job statuses

Retrieve the status of all scheduled crawling jobs across the organization.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await crawlingJobsGetAllCrawlingJobStatus(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
