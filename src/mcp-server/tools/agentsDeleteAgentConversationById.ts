/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsDeleteAgentConversationById } from "../../funcs/agentsDeleteAgentConversationById.js";
import { DeleteAgentConversationByIdRequest$zodSchema } from "../../models/deleteagentconversationbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteAgentConversationByIdRequest$zodSchema,
};

export const tool$agentsDeleteAgentConversationById: ToolDefinition<
  typeof args
> = {
  name: "agents-delete-agent-conversation-by-id",
  description: `Delete an agent conversation

Soft-deletes an agent conversation owned by the authenticated user.

The controller scopes the lookup by \`_id\`, \`orgId\`, \`userId\`, and
\`agentKey\`. If no matching writable conversation is found, the route is
intentionally a no-op and still returns HTTP 200 with \`conversation: null\`.

This makes the operation idempotent:

- deleting a nonexistent conversation returns success with \`null\`
- deleting through a different \`agentKey\` returns success with \`null\`
- deleting an already deleted conversation returns success with \`null\`
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsDeleteAgentConversationById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
