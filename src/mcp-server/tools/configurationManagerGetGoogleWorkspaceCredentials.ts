/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerGetGoogleWorkspaceCredentials } from "../../funcs/configurationManagerGetGoogleWorkspaceCredentials.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$configurationManagerGetGoogleWorkspaceCredentials:
  ToolDefinition = {
    name: "configuration-manager-get-google-workspace-a88",
    description: `Get Google Workspace credentials

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve Google Workspace service account credentials.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    tool: async (client, ctx) => {
      const [result] = await configurationManagerGetGoogleWorkspaceCredentials(
        client,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
