/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentTemplatesGet } from "../../funcs/agentTemplatesGet.js";
import { GetAgentTemplateRequest$zodSchema } from "../../models/getagenttemplateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentTemplateRequest$zodSchema,
};

export const tool$agentTemplatesGet: ToolDefinition<typeof args> = {
  name: "agent-templates-get",
  description: `Get agent template

Retrieve a specific agent template by ID.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentTemplatesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
