/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorOAuthConfigurationCreateGoogleWorkspaceCredentials } from "../../funcs/connectorOAuthConfigurationCreateGoogleWorkspaceCredentials.js";
import { CreateGoogleWorkspaceCredentialsRequest$zodSchema } from "../../models/creategoogleworkspacecredentialsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateGoogleWorkspaceCredentialsRequest$zodSchema,
};

export const tool$connectorOAuthConfigurationCreateGoogleWorkspaceCredentials:
  ToolDefinition<typeof args> = {
    name: "connector-O-auth-configuration-create-google-eb2",
    description: `Upload Google Workspace credentials

Upload Google Workspace credentials (service account JSON for business, or OAuth tokens for individual users). File must be valid JSON.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await connectorOAuthConfigurationCreateGoogleWorkspaceCredentials(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
