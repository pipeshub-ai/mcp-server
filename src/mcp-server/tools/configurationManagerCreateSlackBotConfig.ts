/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerCreateSlackBotConfig } from "../../funcs/configurationManagerCreateSlackBotConfig.js";
import { CreateSlackBotConfigRequest$zodSchema } from "../../models/createslackbotconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSlackBotConfigRequest$zodSchema,
};

export const tool$configurationManagerCreateSlackBotConfig: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-create-slack-bot-config",
  description: `Create Slack bot configuration

Create a new Slack bot configuration for the organization.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerCreateSlackBotConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
