/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crawlingJobsRemoveAllCrawlingJob } from "../../funcs/crawlingJobsRemoveAllCrawlingJob.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$crawlingJobsRemoveAllCrawlingJob: ToolDefinition = {
  name: "crawling-jobs-remove-all-crawling-job",
  description: `Remove all crawling jobs

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Remove all scheduled crawling jobs for the organization.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await crawlingJobsRemoveAllCrawlingJob(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
