/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchUnarchiveSearch } from "../../funcs/semanticSearchUnarchiveSearch.js";
import { UnarchiveSearchRequest$zodSchema } from "../../models/unarchivesearchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnarchiveSearchRequest$zodSchema,
};

export const tool$semanticSearchUnarchiveSearch: ToolDefinition<typeof args> = {
  name: "semantic-search-unarchive-search",
  description: `Unarchive a search

Restore a previously archived search result back to the active search history.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchUnarchiveSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
