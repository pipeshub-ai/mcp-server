/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webSearchGetWebSearchProviders } from "../../funcs/webSearchGetWebSearchProviders.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webSearchGetWebSearchProviders: ToolDefinition = {
  name: "web-search-get-web-search-providers",
  description: `Get all web search providers

Retrieve all configured web search providers and current web search settings.

**Authentication:** Session JWT or OAuth 2.0 access token via \`Authorization: Bearer\`.
OAuth tokens must include the \`config:read\` scope. Admin role is not required.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await webSearchGetWebSearchProviders(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
