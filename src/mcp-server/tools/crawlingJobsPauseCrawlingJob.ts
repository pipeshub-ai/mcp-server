/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crawlingJobsPauseCrawlingJob } from "../../funcs/crawlingJobsPauseCrawlingJob.js";
import { PauseCrawlingJobRequest$zodSchema } from "../../models/pausecrawlingjobop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PauseCrawlingJobRequest$zodSchema,
};

export const tool$crawlingJobsPauseCrawlingJob: ToolDefinition<typeof args> = {
  name: "crawling-jobs-pause-crawling-job",
  description: `Pause a crawling job

Pause a running or scheduled crawling job for a specific connector.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crawlingJobsPauseCrawlingJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
