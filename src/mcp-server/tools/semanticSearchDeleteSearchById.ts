/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchDeleteSearchById } from "../../funcs/semanticSearchDeleteSearchById.js";
import { DeleteSearchByIdRequest$zodSchema } from "../../models/deletesearchbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSearchByIdRequest$zodSchema,
};

export const tool$semanticSearchDeleteSearchById: ToolDefinition<typeof args> =
  {
    name: "semantic-search-delete-search-by-id",
    description: `Delete search by ID

Delete a specific search result by its ID.
`,
    annotations: {
      "title": "",
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await semanticSearchDeleteSearchById(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
