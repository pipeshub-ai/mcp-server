/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetUserTeamsViaUsers } from "../../funcs/usersGetUserTeamsViaUsers.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$usersGetUserTeamsViaUsers: ToolDefinition = {
  name: "users-get-user-teams-via-users",
  description: `Get user teams

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve teams associated with the authenticated user.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await usersGetUserTeamsViaUsers(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
