/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentConversationsGet } from "../../funcs/agentConversationsGet.js";
import { GetAgentConversationRequest$zodSchema } from "../../models/getagentconversationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentConversationRequest$zodSchema,
};

export const tool$agentConversationsGet: ToolDefinition<typeof args> = {
  name: "agent-conversations-get",
  description: `Get agent conversation

Retrieve a specific agent conversation by ID.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentConversationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
