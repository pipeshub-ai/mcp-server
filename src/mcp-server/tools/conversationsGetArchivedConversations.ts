/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsGetArchivedConversations } from "../../funcs/conversationsGetArchivedConversations.js";
import { GetArchivedConversationsRequest$zodSchema } from "../../models/getarchivedconversationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetArchivedConversationsRequest$zodSchema.optional(),
};

export const tool$conversationsGetArchivedConversations: ToolDefinition<
  typeof args
> = {
  name: "conversations-get-archived-conversations",
  description: `List archived conversations

Retrieve all archived conversations for the authenticated user.

**Overview:**

Archived conversations are hidden from the main list but preserved for reference.
This endpoint returns only conversations where \`isArchived: true\` and \`archivedBy\`
is set. Results include conversations the caller owns and those shared with them.

**Filtering and sorting:**

Results can be narrowed using \`search\`, \`shared\`, \`startDate\`, \`endDate\`, and
\`conversationId\`. Sorting is controlled by \`sortBy\` and \`sortOrder\`. Pagination
is controlled by \`page\` and \`limit\`.

**Unarchiving:**

Use \`PATCH /conversations/{conversationId}/unarchive\` to restore a conversation
to the active list.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await conversationsGetArchivedConversations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
