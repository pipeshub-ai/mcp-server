/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsUploadAgentConversationChatAttachments } from "../../funcs/agentsUploadAgentConversationChatAttachments.js";
import { UploadAgentConversationChatAttachmentsRequest$zodSchema } from "../../models/uploadagentconversationchatattachmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadAgentConversationChatAttachmentsRequest$zodSchema,
};

export const tool$agentsUploadAgentConversationChatAttachments: ToolDefinition<
  typeof args
> = {
  name: "agents-upload-agent-conversation-chat-attachments",
  description: `Upload agent chat attachments

Multipart upload of PDF, JPEG, or PNG files for agent chat. Same limits as assistant
chat (\`POST /conversations/attachments/upload\`): up to 10 files, 5 MiB each. Proxies to
the AI backend. Optional \`conversationId\` associates uploads with an existing agent thread.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsUploadAgentConversationChatAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
