/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentManagementDeleteDocumentById } from "../../funcs/documentManagementDeleteDocumentById.js";
import { DeleteDocumentByIdRequest$zodSchema } from "../../models/deletedocumentbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteDocumentByIdRequest$zodSchema,
};

export const tool$documentManagementDeleteDocumentById: ToolDefinition<
  typeof args
> = {
  name: "document-management-delete-document-by-id",
  description: `Delete document by ID

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Delete a document by its ID.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await documentManagementDeleteDocumentById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
