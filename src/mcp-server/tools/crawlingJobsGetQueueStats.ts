/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crawlingJobsGetQueueStats } from "../../funcs/crawlingJobsGetQueueStats.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$crawlingJobsGetQueueStats: ToolDefinition = {
  name: "crawling-jobs-get-queue-stats",
  description: `Get queue statistics

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve statistics for the crawling job queue including active, waiting, and completed job counts.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await crawlingJobsGetQueueStats(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
