/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { samlSAMLSignInCallback } from "../../funcs/samlSAMLSignInCallback.js";
import { SamlSignInCallbackRequest$zodSchema } from "../../models/samlsignincallbackop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SamlSignInCallbackRequest$zodSchema.optional(),
};

export const tool$samlSAMLSignInCallback: ToolDefinition<typeof args> = {
  name: "saml-saml-sign-in-callback",
  description: `SAML sign-in callback

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Handle the SAML Identity Provider callback after user authentication. This endpoint receives the SAML assertion from the IdP.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await samlSAMLSignInCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
