/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiModelsProvidersSetDefaultAIModel } from "../../funcs/aiModelsProvidersSetDefaultAIModel.js";
import { SetDefaultAIModelRequest$zodSchema } from "../../models/setdefaultaimodelop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetDefaultAIModelRequest$zodSchema,
};

export const tool$aiModelsProvidersSetDefaultAIModel: ToolDefinition<
  typeof args
> = {
  name: "AI-models-providers-set-default-AI-model",
  description: `Set default AI model

Set a model as the default for its type.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await aiModelsProvidersSetDefaultAIModel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
