/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerSetMetricsCollectionPushInterval } from "../../funcs/configurationManagerSetMetricsCollectionPushInterval.js";
import { SetMetricsCollectionPushIntervalRequest$zodSchema } from "../../models/setmetricscollectionpushintervalop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetMetricsCollectionPushIntervalRequest$zodSchema,
};

export const tool$configurationManagerSetMetricsCollectionPushInterval:
  ToolDefinition<typeof args> = {
    name: "configuration-manager-set-metrics-collection-cb8",
    description: `Set metrics push interval

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Configure the interval for pushing metrics to the collection server.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await configurationManagerSetMetricsCollectionPushInterval(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
