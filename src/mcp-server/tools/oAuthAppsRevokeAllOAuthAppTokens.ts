/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsRevokeAllOAuthAppTokens } from "../../funcs/oAuthAppsRevokeAllOAuthAppTokens.js";
import { RevokeAllOAuthAppTokensRequest$zodSchema } from "../../models/revokealloauthapptokensop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: RevokeAllOAuthAppTokensRequest$zodSchema,
};

export const tool$oAuthAppsRevokeAllOAuthAppTokens: ToolDefinition<
  typeof args
> = {
  name: "O-auth-apps-revoke-all-O-auth-app-tokens",
  description: `Revoke all app tokens

Revokes every access and refresh token currently issued to an OAuth app, in a single operation. Use this for emergency credential rotation, suspected secret leaks, or as a follow-up to \`POST /oauth-clients/{appId}/regenerate-secret\` when you want existing sessions invalidated immediately rather than letting them expire naturally.

The response \`count\` is the total number of tokens revoked across both types. Clients of this app must then obtain new tokens via the standard OAuth flow.

Creator-only.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsRevokeAllOAuthAppTokens(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
