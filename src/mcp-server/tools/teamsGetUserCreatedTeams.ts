/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetUserCreatedTeams } from "../../funcs/teamsGetUserCreatedTeams.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$teamsGetUserCreatedTeams: ToolDefinition = {
  name: "teams-get-user-created-teams",
  description: `Get user created teams

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve teams created by the authenticated user.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await teamsGetUserCreatedTeams(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
