/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsUpdatePermissions } from "../../funcs/agentsUpdatePermissions.js";
import { UpdateAgentPermissionsRequest$zodSchema } from "../../models/updateagentpermissionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateAgentPermissionsRequest$zodSchema,
};

export const tool$agentsUpdatePermissions: ToolDefinition<typeof args> = {
  name: "agents-update-permissions",
  description: `Update agent permissions

Update who can access and use the agent.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsUpdatePermissions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
