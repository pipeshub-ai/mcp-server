/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchDeleteAllSearchHistory } from "../../funcs/semanticSearchDeleteAllSearchHistory.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$semanticSearchDeleteAllSearchHistory: ToolDefinition = {
  name: "semantic-search-delete-all-search-history",
  description: `Clear all search history

Delete all search history for the authenticated user.<br><br>
<b>Warning:</b><br>
This action cannot be undone. All saved searches will be permanently removed.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result] = await semanticSearchDeleteAllSearchHistory(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
