/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsUpdate } from "../../funcs/permissionsUpdate.js";
import { UpdateKBPermissionsRequest$zodSchema } from "../../models/updatekbpermissionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateKBPermissionsRequest$zodSchema,
};

export const tool$permissionsUpdate: ToolDefinition<typeof args> = {
  name: "permissions-update",
  description: `Update permissions

Update permission roles for users or teams.<br><br>
<b>Required Permission:</b> OWNER or ORGANIZER
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await permissionsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
