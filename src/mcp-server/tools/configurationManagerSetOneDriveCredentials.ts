/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerSetOneDriveCredentials } from "../../funcs/configurationManagerSetOneDriveCredentials.js";
import { SetOneDriveCredentialsRequest$zodSchema } from "../../models/setonedrivecredentialsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetOneDriveCredentialsRequest$zodSchema,
};

export const tool$configurationManagerSetOneDriveCredentials: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-set-one-drive-credentials",
  description: `Set OneDrive credentials

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Configure OneDrive connector credentials.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerSetOneDriveCredentials(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
