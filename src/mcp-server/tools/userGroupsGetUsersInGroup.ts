/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userGroupsGetUsersInGroup } from "../../funcs/userGroupsGetUsersInGroup.js";
import { GetUsersInGroupRequest$zodSchema } from "../../models/getusersingroupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetUsersInGroupRequest$zodSchema,
};

export const tool$userGroupsGetUsersInGroup: ToolDefinition<typeof args> = {
  name: "user-groups-get-users-in-group",
  description: `Get users in group

Retrieve all users that belong to a specific user group.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await userGroupsGetUsersInGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
