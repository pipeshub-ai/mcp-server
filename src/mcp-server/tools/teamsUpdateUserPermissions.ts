/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsUpdateUserPermissions } from "../../funcs/teamsUpdateUserPermissions.js";
import { UpdateTeamUserPermissionsRequest$zodSchema } from "../../models/updateteamuserpermissionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateTeamUserPermissionsRequest$zodSchema,
};

export const tool$teamsUpdateUserPermissions: ToolDefinition<typeof args> = {
  name: "teams-update-user-permissions",
  description: `Update user role in team

Update a user's role/permissions within a team.<br><br>
<b>Available Roles:</b><br>
<ul>
<li><code>owner</code> - Full control (only one per team, transferable)</li>
<li><code>admin</code> - Can manage members and settings</li>
<li><code>member</code> - Standard access</li>
<li><code>viewer</code> - Read-only access</li>
</ul>
<b>Restrictions:</b><br>
<ul>
<li>Only team owner can promote to admin</li>
<li>Only team owner can transfer ownership</li>
<li>Admins can modify member/viewer roles</li>
</ul>
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await teamsUpdateUserPermissions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
