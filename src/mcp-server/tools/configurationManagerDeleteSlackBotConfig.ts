/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerDeleteSlackBotConfig } from "../../funcs/configurationManagerDeleteSlackBotConfig.js";
import { DeleteSlackBotConfigRequest$zodSchema } from "../../models/deleteslackbotconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSlackBotConfigRequest$zodSchema,
};

export const tool$configurationManagerDeleteSlackBotConfig: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-delete-slack-bot-config",
  description: `Delete Slack bot configuration

Delete a Slack bot configuration.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerDeleteSlackBotConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
