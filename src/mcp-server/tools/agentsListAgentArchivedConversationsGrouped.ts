/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsListAgentArchivedConversationsGrouped } from "../../funcs/agentsListAgentArchivedConversationsGrouped.js";
import { ListAgentArchivedConversationsGroupedRequest$zodSchema } from "../../models/listagentarchivedconversationsgroupedop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListAgentArchivedConversationsGroupedRequest$zodSchema.optional(),
};

export const tool$agentsListAgentArchivedConversationsGrouped: ToolDefinition<
  typeof args
> = {
  name: "agents-list-agent-archived-conversations-grouped",
  description: `List archived agent conversations grouped by agent

Returns archived agent conversations for the current user, grouped by
\`agentKey\`, with pagination over agent groups. Excludes conversations
whose agent was soft-deleted upstream.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsListAgentArchivedConversationsGrouped(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
