/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthConfigurationDeleteToolsetOAuthConfig } from "../../funcs/oAuthConfigurationDeleteToolsetOAuthConfig.js";
import { DeleteToolsetOAuthConfigRequest$zodSchema } from "../../models/deletetoolsetoauthconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteToolsetOAuthConfigRequest$zodSchema,
};

export const tool$oAuthConfigurationDeleteToolsetOAuthConfig: ToolDefinition<
  typeof args
> = {
  name: "O-auth-configuration-delete-toolset-O-auth-config",
  description: `Delete OAuth config`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthConfigurationDeleteToolsetOAuthConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
