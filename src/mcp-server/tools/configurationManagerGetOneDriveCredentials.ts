/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerGetOneDriveCredentials } from "../../funcs/configurationManagerGetOneDriveCredentials.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$configurationManagerGetOneDriveCredentials: ToolDefinition = {
  name: "configuration-manager-get-one-drive-credentials",
  description: `Get OneDrive credentials

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve OneDrive connector credentials.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await configurationManagerGetOneDriveCredentials(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
