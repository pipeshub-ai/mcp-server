/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { platformSettingsGetAvailableFeatureFlags } from "../../funcs/platformSettingsGetAvailableFeatureFlags.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$platformSettingsGetAvailableFeatureFlags: ToolDefinition = {
  name: "platform-settings-get-available-feature-flags",
  description: `Get available feature flags

List all available feature flags with their descriptions and default values.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await platformSettingsGetAvailableFeatureFlags(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
