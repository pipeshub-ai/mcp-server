/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authenticationConfigurationGetSso } from "../../funcs/authenticationConfigurationGetSso.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authenticationConfigurationGetSso: ToolDefinition = {
  name: "authentication-configuration-get-sso",
  description: `Get SAML SSO configuration

Get SAML SSO configuration.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await authenticationConfigurationGetSso(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
