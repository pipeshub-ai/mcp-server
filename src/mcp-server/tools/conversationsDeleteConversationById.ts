/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsDeleteConversationById } from "../../funcs/conversationsDeleteConversationById.js";
import { DeleteConversationByIdRequest$zodSchema } from "../../models/deleteconversationbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteConversationByIdRequest$zodSchema,
};

export const tool$conversationsDeleteConversationById: ToolDefinition<
  typeof args
> = {
  name: "conversations-delete-conversation-by-id",
  description: `Delete conversation

Delete a conversation by its ID.

**Overview:**

Performs a soft delete by setting \`isDeleted: true\`. The conversation is
removed from listings but preserved in the database. All citations
referenced by messages in the conversation are also soft-deleted.

**Permissions:**

The conversation initiator can always delete. Users the conversation has
been shared with may delete it only when their \`sharedWith.accessLevel\`
is \`write\`.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await conversationsDeleteConversationById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
