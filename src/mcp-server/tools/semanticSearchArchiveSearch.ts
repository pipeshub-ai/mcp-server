/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchArchiveSearch } from "../../funcs/semanticSearchArchiveSearch.js";
import { ArchiveSearchRequest$zodSchema } from "../../models/archivesearchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ArchiveSearchRequest$zodSchema,
};

export const tool$semanticSearchArchiveSearch: ToolDefinition<typeof args> = {
  name: "semantic-search-archive-search",
  description: `Archive a search

Archive a specific search result. Archived searches are hidden
from the default search history view but remain retrievable via
the archive-aware listing endpoints.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchArchiveSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
