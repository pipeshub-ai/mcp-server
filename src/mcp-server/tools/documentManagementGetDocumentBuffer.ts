/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentManagementGetDocumentBuffer } from "../../funcs/documentManagementGetDocumentBuffer.js";
import { GetDocumentBufferRequest$zodSchema } from "../../models/getdocumentbufferop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetDocumentBufferRequest$zodSchema,
};

export const tool$documentManagementGetDocumentBuffer: ToolDefinition<
  typeof args
> = {
  name: "document-management-get-document-buffer",
  description: `Get document buffer

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve the binary buffer of a document.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await documentManagementGetDocumentBuffer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
