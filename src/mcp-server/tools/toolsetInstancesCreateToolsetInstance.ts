/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetInstancesCreateToolsetInstance } from "../../funcs/toolsetInstancesCreateToolsetInstance.js";
import { CreateToolsetInstanceRequest$zodSchema } from "../../models/createtoolsetinstanceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateToolsetInstanceRequest$zodSchema,
};

export const tool$toolsetInstancesCreateToolsetInstance: ToolDefinition<
  typeof args
> = {
  name: "toolset-instances-create-toolset-instance",
  description: `Create toolset instance

Create a new toolset instance (admin only).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetInstancesCreateToolsetInstance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
