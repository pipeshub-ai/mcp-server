/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerSetMetricsCollectionRemoteServer } from "../../funcs/configurationManagerSetMetricsCollectionRemoteServer.js";
import { SetMetricsCollectionRemoteServerRequest$zodSchema } from "../../models/setmetricscollectionremoteserverop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetMetricsCollectionRemoteServerRequest$zodSchema,
};

export const tool$configurationManagerSetMetricsCollectionRemoteServer:
  ToolDefinition<typeof args> = {
    name: "configuration-manager-set-metrics-collection-58e",
    description: `Set metrics remote server URL

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Configure the remote server URL for metrics collection.
`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await configurationManagerSetMetricsCollectionRemoteServer(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
