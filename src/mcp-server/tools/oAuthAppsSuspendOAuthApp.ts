/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsSuspendOAuthApp } from "../../funcs/oAuthAppsSuspendOAuthApp.js";
import { SuspendOAuthAppRequest$zodSchema } from "../../models/suspendoauthappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SuspendOAuthAppRequest$zodSchema,
};

export const tool$oAuthAppsSuspendOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-suspend-O-auth-app",
  description: `Suspend OAuth app

Moves an OAuth app to \`status: "suspended"\`, blocking new token issuance at \`POST /oauth2/token\` and the authorization-code consent flow. Tokens that have already been issued remain valid until their natural expiry — call \`POST /oauth-clients/{appId}/revoke-all-tokens\` immediately afterwards if you need an immediate lockout.

Use this for temporary suspensions where you intend to reactivate later. For permanent removal, use \`DELETE /oauth-clients/{appId}\`. Suspending an app that is already suspended returns \`400\`.

Creator-only.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsSuspendOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
