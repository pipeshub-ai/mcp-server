/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsGetOAuthApp } from "../../funcs/oAuthAppsGetOAuthApp.js";
import { GetOAuthAppRequest$zodSchema } from "../../models/getoauthappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetOAuthAppRequest$zodSchema,
};

export const tool$oAuthAppsGetOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-get-O-auth-app",
  description: `Get OAuth app details

Returns the full configuration of an OAuth app you registered. The \`clientSecret\` is never echoed back here; if you need a new one, call \`POST /oauth-clients/{appId}/regenerate-secret\`.

Access is creator-scoped: even org admins receive \`404\` for apps owned by other users. This avoids leaking app metadata across org members and keeps the read surface symmetric with \`listOAuthApps\`.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsGetOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
