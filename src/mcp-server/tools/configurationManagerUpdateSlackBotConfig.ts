/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerUpdateSlackBotConfig } from "../../funcs/configurationManagerUpdateSlackBotConfig.js";
import { UpdateSlackBotConfigRequest$zodSchema } from "../../models/updateslackbotconfigop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateSlackBotConfigRequest$zodSchema,
};

export const tool$configurationManagerUpdateSlackBotConfig: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-update-slack-bot-config",
  description: `Update Slack bot configuration

Update an existing Slack bot configuration.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerUpdateSlackBotConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
