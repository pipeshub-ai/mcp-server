/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authenticationConfigurationGetAzureAd } from "../../funcs/authenticationConfigurationGetAzureAd.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authenticationConfigurationGetAzureAd: ToolDefinition = {
  name: "authentication-configuration-get-azure-ad",
  description: `Get Azure AD configuration

Retrieve Azure AD authentication configuration.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await authenticationConfigurationGetAzureAd(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
