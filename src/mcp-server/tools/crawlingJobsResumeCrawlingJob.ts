/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crawlingJobsResumeCrawlingJob } from "../../funcs/crawlingJobsResumeCrawlingJob.js";
import { ResumeCrawlingJobRequest$zodSchema } from "../../models/resumecrawlingjobop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ResumeCrawlingJobRequest$zodSchema,
};

export const tool$crawlingJobsResumeCrawlingJob: ToolDefinition<typeof args> = {
  name: "crawling-jobs-resume-crawling-job",
  description: `Resume a crawling job

Resume a previously paused crawling job for a specific connector.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crawlingJobsResumeCrawlingJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
