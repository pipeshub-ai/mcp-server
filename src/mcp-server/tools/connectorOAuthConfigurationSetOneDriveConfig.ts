/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorOAuthConfigurationSetOneDriveConfig } from "../../funcs/connectorOAuthConfigurationSetOneDriveConfig.js";
import { OneDriveConfig$zodSchema } from "../../models/onedriveconfig.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OneDriveConfig$zodSchema,
};

export const tool$connectorOAuthConfigurationSetOneDriveConfig: ToolDefinition<
  typeof args
> = {
  name: "connector-O-auth-configuration-set-one-drive-config",
  description: `Configure OneDrive connector

Set up Microsoft credentials for OneDrive connector.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorOAuthConfigurationSetOneDriveConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
