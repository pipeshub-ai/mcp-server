/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { foldersCreateSubfolder } from "../../funcs/foldersCreateSubfolder.js";
import { CreateSubfolderRequest$zodSchema } from "../../models/createsubfolderop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreateSubfolderRequest$zodSchema,
};

export const tool$foldersCreateSubfolder: ToolDefinition<typeof args> = {
  name: "folders-create-subfolder",
  description: `Create subfolder

Create a nested folder within an existing folder.<br><br>
<b>Required Permission:</b> FILEORGANIZER or higher<br><br>
<b>Nesting:</b><br>
Supports unlimited folder nesting depth for complex organizational structures.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await foldersCreateSubfolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
