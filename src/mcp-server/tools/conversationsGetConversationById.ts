/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { conversationsGetConversationById } from "../../funcs/conversationsGetConversationById.js";
import { GetConversationByIdRequest$zodSchema } from "../../models/getconversationbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetConversationByIdRequest$zodSchema,
};

export const tool$conversationsGetConversationById: ToolDefinition<
  typeof args
> = {
  name: "conversations-get-conversation-by-id",
  description: `Get conversation by ID

Retrieve a specific conversation with its full message history.

**Overview:**

Returns the complete conversation including all messages, citations,
feedback, and metadata. Messages can be paginated for long conversations.

**Message Pagination:**

For conversations with many messages, use pagination parameters:

- \`page\`: Page number (default: 1)
- \`limit\`: Messages per page (default: 10)
- \`sortBy\`: Sort field (default: createdAt)
- \`sortOrder\`: 'asc' or 'desc' (default: desc)

**Access Control:**

Users can access conversations they own or that have been shared with them.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await conversationsGetConversationById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
