/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userAccountResetPassword } from "../../funcs/userAccountResetPassword.js";
import { ResetPasswordRequest$zodSchema } from "../../models/resetpasswordop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ResetPasswordRequest$zodSchema,
};

export const tool$userAccountResetPassword: ToolDefinition<typeof args> = {
  name: "user-account-reset-password",
  description: `Reset password

Reset the password for the currently authenticated user.

**Overview:**

Allows a logged-in user to change their password by providing the current password and a new password.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await userAccountResetPassword(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
