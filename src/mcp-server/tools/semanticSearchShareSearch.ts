/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchShareSearch } from "../../funcs/semanticSearchShareSearch.js";
import { ShareSearchRequest$zodSchema } from "../../models/sharesearchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ShareSearchRequest$zodSchema,
};

export const tool$semanticSearchShareSearch: ToolDefinition<typeof args> = {
  name: "semantic-search-share-search",
  description: `Share a search

Share a specific search result, making it accessible to other users.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchShareSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
