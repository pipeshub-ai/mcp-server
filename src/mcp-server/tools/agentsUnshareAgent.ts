/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsUnshareAgent } from "../../funcs/agentsUnshareAgent.js";
import { UnshareAgentRequest$zodSchema } from "../../models/unshareagentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UnshareAgentRequest$zodSchema,
};

export const tool$agentsUnshareAgent: ToolDefinition<typeof args> = {
  name: "agents-unshare-agent",
  description: `Unshare an agent

Revoke sharing for an agent, removing access for specified users or teams.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await agentsUnshareAgent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
