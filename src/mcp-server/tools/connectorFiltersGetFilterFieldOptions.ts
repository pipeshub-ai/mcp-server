/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorFiltersGetFilterFieldOptions } from "../../funcs/connectorFiltersGetFilterFieldOptions.js";
import { GetFilterFieldOptionsRequest$zodSchema } from "../../models/getfilterfieldoptionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetFilterFieldOptionsRequest$zodSchema,
};

export const tool$connectorFiltersGetFilterFieldOptions: ToolDefinition<
  typeof args
> = {
  name: "connector-filters-get-filter-field-options",
  description: `Get dynamic filter options

Get options for a dynamic filter field with pagination.<br><br>
<b>Overview:</b><br>
For filters with <code>dynamic: true</code>, options are loaded
from the connected service. This supports pagination and search.<br><br>
<b>Examples:</b><br>
<ul>
<li>Google Drive folders list</li>
<li>Slack channels list</li>
<li>Confluence spaces list</li>
</ul>
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorFiltersGetFilterFieldOptions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
