/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetTeamUsers } from "../../funcs/teamsGetTeamUsers.js";
import { GetTeamUsersRequest$zodSchema } from "../../models/getteamusersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTeamUsersRequest$zodSchema,
};

export const tool$teamsGetTeamUsers: ToolDefinition<typeof args> = {
  name: "teams-get-team-users",
  description: `Get users in team

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Retrieve all users that belong to a specific team.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await teamsGetTeamUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
