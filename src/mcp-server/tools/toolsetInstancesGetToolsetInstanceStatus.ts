/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { toolsetInstancesGetToolsetInstanceStatus } from "../../funcs/toolsetInstancesGetToolsetInstanceStatus.js";
import { GetToolsetInstanceStatusRequest$zodSchema } from "../../models/gettoolsetinstancestatusop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetToolsetInstanceStatusRequest$zodSchema,
};

export const tool$toolsetInstancesGetToolsetInstanceStatus: ToolDefinition<
  typeof args
> = {
  name: "toolset-instances-get-toolset-instance-status",
  description: `Get instance authentication status`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await toolsetInstancesGetToolsetInstanceStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
