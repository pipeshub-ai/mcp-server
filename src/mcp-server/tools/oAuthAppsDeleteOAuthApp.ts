/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthAppsDeleteOAuthApp } from "../../funcs/oAuthAppsDeleteOAuthApp.js";
import { DeleteOAuthAppRequest$zodSchema } from "../../models/deleteoauthappop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteOAuthAppRequest$zodSchema,
};

export const tool$oAuthAppsDeleteOAuthApp: ToolDefinition<typeof args> = {
  name: "O-auth-apps-delete-O-auth-app",
  description: `Delete OAuth app

Soft-deletes an OAuth app. The app is flagged \`isDeleted=true\` on the \`OAuthApp\` document, removed from list/get responses for every caller, and all of its access and refresh tokens are revoked in the same operation. There is no restore endpoint — deletion is final.

Creator-only: even org admins cannot delete apps owned by other users.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthAppsDeleteOAuthApp(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
