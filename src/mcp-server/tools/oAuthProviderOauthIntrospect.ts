/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuthProviderOauthIntrospect } from "../../funcs/oAuthProviderOauthIntrospect.js";
import { OAuthIntrospectRequest$zodSchema } from "../../models/oauthintrospectrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: OAuthIntrospectRequest$zodSchema,
};

export const tool$oAuthProviderOauthIntrospect: ToolDefinition<typeof args> = {
  name: "O-auth-provider-oauth-introspect",
  description: `Introspect a token

OAuth 2.0 Token Introspection Endpoint (RFC 7662).

Check if a token is active and retrieve its metadata.

**Use Cases:**
- Resource servers validating tokens
- Debugging token issues
- Checking token scopes before processing requests

**Response:**
- Active token: Returns \`active: true\` with token metadata
- Invalid/expired/revoked token: Returns only \`active: false\`
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await oAuthProviderOauthIntrospect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
