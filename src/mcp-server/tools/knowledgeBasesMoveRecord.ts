/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeBasesMoveRecord } from "../../funcs/knowledgeBasesMoveRecord.js";
import { MoveRecordRequest$zodSchema } from "../../models/moverecordop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: MoveRecordRequest$zodSchema,
};

export const tool$knowledgeBasesMoveRecord: ToolDefinition<typeof args> = {
  name: "knowledge-bases-move-record",
  description: `Move record to another location

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Move a record from one location to another within a knowledge base.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await knowledgeBasesMoveRecord(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
