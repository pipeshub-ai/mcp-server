/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUpdateEmail } from "../../funcs/usersUpdateEmail.js";
import { UpdateEmailRequest$zodSchema } from "../../models/updateemailop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateEmailRequest$zodSchema,
};

export const tool$usersUpdateEmail: ToolDefinition<typeof args> = {
  name: "users-update-email",
  description: `Update user email

Update the email address of a user.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersUpdateEmail(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
