/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { uploadsToFolder } from "../../funcs/uploadsToFolder.js";
import { UploadRecordsToFolderRequest$zodSchema } from "../../models/uploadrecordstofolderop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UploadRecordsToFolderRequest$zodSchema,
};

export const tool$uploadsToFolder: ToolDefinition<typeof args> = {
  name: "uploads-to-folder",
  description: `Upload files to folder

Upload files directly to a specific folder within a knowledge base.<br><br>
<b>Same as KB upload</b> but files are placed in the specified folder instead of KB root.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await uploadsToFolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
