/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { configurationManagerSetSharePointCredentials } from "../../funcs/configurationManagerSetSharePointCredentials.js";
import { SetSharePointCredentialsRequest$zodSchema } from "../../models/setsharepointcredentialsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: SetSharePointCredentialsRequest$zodSchema,
};

export const tool$configurationManagerSetSharePointCredentials: ToolDefinition<
  typeof args
> = {
  name: "configuration-manager-set-share-point-credentials",
  description: `Set SharePoint credentials

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Configure SharePoint connector credentials.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await configurationManagerSetSharePointCredentials(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
