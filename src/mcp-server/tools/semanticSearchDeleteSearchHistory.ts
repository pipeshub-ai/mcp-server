/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { semanticSearchDeleteSearchHistory } from "../../funcs/semanticSearchDeleteSearchHistory.js";
import { DeleteSearchHistoryRequest$zodSchema } from "../../models/deletesearchhistoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteSearchHistoryRequest$zodSchema.optional(),
};

export const tool$semanticSearchDeleteSearchHistory: ToolDefinition<
  typeof args
> = {
  name: "semantic-search-delete-search-history",
  description: `Clear all search history

Permanently delete every persisted search row owned by, or shared
with, the authenticated user, along with the citation rows those
searches reference. The action cannot be undone.

Scoped to the caller's org and limited to rows where
\`isDeleted: false\` and \`isArchived: false\`. If nothing matches
(including the case where every row is already archived), the
endpoint returns \`404\` rather than a successful no-op.
`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await semanticSearchDeleteSearchHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
