/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentManagementCreatePlaceholderDocument } from "../../funcs/documentManagementCreatePlaceholderDocument.js";
import { CreatePlaceholderDocumentRequest$zodSchema } from "../../models/createplaceholderdocumentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreatePlaceholderDocumentRequest$zodSchema,
};

export const tool$documentManagementCreatePlaceholderDocument: ToolDefinition<
  typeof args
> = {
  name: "document-management-create-placeholder-document",
  description: `Create placeholder document

<b>⚠️ Deprecated:</b> This endpoint is deprecated and will be removed in a future release.<br><br>
Create a placeholder document entry without uploading content.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await documentManagementCreatePlaceholderDocument(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
