/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * `requestId` comes from `req.context?.requestId` and is omitted from
 *
 * @remarks
 * the JSON when upstream middleware did not set it.
 */
export type SemanticSearchHistoryMeta = {
  requestId?: string | undefined;
  timestamp: string;
  duration: number;
};

export const SemanticSearchHistoryMeta$zodSchema: z.ZodType<
  SemanticSearchHistoryMeta
> = z.object({
  duration: z.int(),
  requestId: z.string().optional(),
  timestamp: z.iso.datetime({ offset: true }),
}).describe(
  "`requestId` comes from `req.context?.requestId` and is omitted from\nthe JSON when upstream middleware did not set it.\n",
);
