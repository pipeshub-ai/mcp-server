/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  SemanticSearchAppliedFilters,
  SemanticSearchAppliedFilters$zodSchema,
} from "./semanticsearchappliedfilters.js";
import {
  SemanticSearchGraphRecord,
  SemanticSearchGraphRecord$zodSchema,
} from "./semanticsearchgraphrecord.js";
import {
  SemanticSearchHit,
  SemanticSearchHit$zodSchema,
} from "./semanticsearchhit.js";

/**
 * Payload returned by the AI retrieval service for a semantic search (embedded in `searchResponse`).
 *
 * @remarks
 * Optional `virtual_to_record_map` maps each virtual record id (string key) to one resolved graph record document.
 * Empty responses from the retrieval layer omit `virtual_to_record_map`; success payloads may include it alongside hits and records.
 */
export type SemanticSearchAiResponse = {
  searchResults: Array<SemanticSearchHit>;
  records: Array<SemanticSearchGraphRecord>;
  status: string;
  status_code: number;
  message: string;
  appliedFilters?: SemanticSearchAppliedFilters | undefined;
  virtual_to_record_map?:
    | { [k: string]: SemanticSearchGraphRecord }
    | undefined;
};

export const SemanticSearchAiResponse$zodSchema: z.ZodType<
  SemanticSearchAiResponse
> = z.object({
  appliedFilters: SemanticSearchAppliedFilters$zodSchema.optional(),
  message: z.string(),
  records: z.array(SemanticSearchGraphRecord$zodSchema),
  searchResults: z.array(SemanticSearchHit$zodSchema),
  status: z.string(),
  status_code: z.int(),
  virtual_to_record_map: z.record(
    z.string(),
    SemanticSearchGraphRecord$zodSchema,
  ).optional(),
}).describe(
  "Payload returned by the AI retrieval service for a semantic search (embedded in `searchResponse`).\nOptional `virtual_to_record_map` maps each virtual record id (string key) to one resolved graph record document.\nEmpty responses from the retrieval layer omit `virtual_to_record_map`; success payloads may include it alongside hits and records.\n",
);
