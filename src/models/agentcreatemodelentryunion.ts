/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type AgentCreateModelEntry = {
  modelKey: string;
  modelName?: string | undefined;
  provider?: string | undefined;
  isReasoning?: boolean | undefined;
};

export const AgentCreateModelEntry$zodSchema: z.ZodType<AgentCreateModelEntry> =
  z.object({
    isReasoning: z.boolean().optional(),
    modelKey: z.string(),
    modelName: z.string().optional(),
    provider: z.string().optional(),
  });

/**
 * Accepted model entry for `POST /agents/create`.
 *
 * @remarks
 * The gateway accepts either a non-empty string model entry or an object entry
 * with a required `modelKey`.
 *
 * The `models` array must include at least one object entry with `isReasoning: true`.
 * String-only entries are schema-valid but are rejected at the gateway with HTTP 400.
 */
export type AgentCreateModelEntryUnion = AgentCreateModelEntry | string;

export const AgentCreateModelEntryUnion$zodSchema: z.ZodType<
  AgentCreateModelEntryUnion
> = z.union([
  z.lazy(() => AgentCreateModelEntry$zodSchema),
  z.string(),
]).describe(
  "Accepted model entry for `POST /agents/create`.\nThe gateway accepts either a non-empty string model entry or an object entry\nwith a required `modelKey`.\n\nThe `models` array must include at least one object entry with `isReasoning: true`.\nString-only entries are schema-valid but are rejected at the gateway with HTTP 400.\n",
);
