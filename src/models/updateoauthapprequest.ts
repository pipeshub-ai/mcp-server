/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const UpdateOAuthAppRequestAllowedGrantType = {
  AuthorizationCode: "authorization_code",
  ClientCredentials: "client_credentials",
  RefreshToken: "refresh_token",
} as const;
export type UpdateOAuthAppRequestAllowedGrantType = ClosedEnum<
  typeof UpdateOAuthAppRequestAllowedGrantType
>;

export const UpdateOAuthAppRequestAllowedGrantType$zodSchema = z.enum([
  "authorization_code",
  "client_credentials",
  "refresh_token",
]);

/**
 * Request to update an OAuth app (`updateAppSchema` in `oauth.validators.ts`).
 *
 * @remarks
 *
 * All fields are optional — include only fields that should change. URL fields (`homepageUrl`, `privacyPolicyUrl`, `termsOfServiceUrl`) accept `null` to clear them (nullable in Zod).
 *
 * **Redirect rule (Zod refine):** If `allowedGrantTypes` includes `authorization_code` and `redirectUris` is present in the body, `redirectUris` must contain at least one URI.
 */
export type UpdateOAuthAppRequest = {
  name?: string | undefined;
  description?: string | undefined;
  redirectUris?: Array<string> | undefined;
  allowedGrantTypes?: Array<UpdateOAuthAppRequestAllowedGrantType> | undefined;
  allowedScopes?: Array<string> | undefined;
  homepageUrl?: string | null | undefined;
  privacyPolicyUrl?: string | null | undefined;
  termsOfServiceUrl?: string | null | undefined;
  accessTokenLifetime?: number | undefined;
  refreshTokenLifetime?: number | undefined;
};

export const UpdateOAuthAppRequest$zodSchema: z.ZodType<UpdateOAuthAppRequest> =
  z.object({
    accessTokenLifetime: z.int().optional(),
    allowedGrantTypes: z.array(UpdateOAuthAppRequestAllowedGrantType$zodSchema)
      .optional(),
    allowedScopes: z.array(z.string()).optional(),
    description: z.string().optional(),
    homepageUrl: z.string().nullable().optional(),
    name: z.string().optional(),
    privacyPolicyUrl: z.string().nullable().optional(),
    redirectUris: z.array(z.string()).optional(),
    refreshTokenLifetime: z.int().optional(),
    termsOfServiceUrl: z.string().nullable().optional(),
  }).describe(
    "Request to update an OAuth app (`updateAppSchema` in `oauth.validators.ts`).\n\nAll fields are optional — include only fields that should change. URL fields (`homepageUrl`, `privacyPolicyUrl`, `termsOfServiceUrl`) accept `null` to clear them (nullable in Zod).\n\n**Redirect rule (Zod refine):** If `allowedGrantTypes` includes `authorization_code` and `redirectUris` is present in the body, `redirectUris` must contain at least one URI.\n",
  );
