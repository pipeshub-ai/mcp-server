/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const UserGroupRefType = {
  Admin: "admin",
  Standard: "standard",
  Everyone: "everyone",
  Custom: "custom",
} as const;
export type UserGroupRefType = ClosedEnum<typeof UserGroupRefType>;

export const UserGroupRefType$zodSchema = z.enum([
  "admin",
  "standard",
  "everyone",
  "custom",
]);

/**
 * Compact reference to a user group as embedded in user list responses.
 *
 * @remarks
 * For the full group document use `GET /userGroups/{id}` /
 * the `UserGroup` schema.
 */
export type UserGroupRef = {
  _id?: string | undefined;
  name?: string | undefined;
  type?: UserGroupRefType | undefined;
};

export const UserGroupRef$zodSchema: z.ZodType<UserGroupRef> = z.object({
  _id: z.string().optional(),
  name: z.string().optional(),
  type: UserGroupRefType$zodSchema.optional(),
}).describe(
  "Compact reference to a user group as embedded in user list responses.\nFor the full group document use `GET /userGroups/{id}` /\nthe `UserGroup` schema.\n",
);
