/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const PersistedSemanticSearchSharedWithEntryAccessLevel = {
  Read: "read",
  Write: "write",
} as const;
export type PersistedSemanticSearchSharedWithEntryAccessLevel = ClosedEnum<
  typeof PersistedSemanticSearchSharedWithEntryAccessLevel
>;

export const PersistedSemanticSearchSharedWithEntryAccessLevel$zodSchema = z
  .enum([
    "read",
    "write",
  ]);

/**
 * Entry inside `sharedWith[]`. The schema sets `_id: false` on these
 *
 * @remarks
 * sub-docs, so no auto-injected `_id` is present.
 */
export type PersistedSemanticSearchSharedWithEntry = {
  userId: string;
  accessLevel: PersistedSemanticSearchSharedWithEntryAccessLevel;
};

export const PersistedSemanticSearchSharedWithEntry$zodSchema: z.ZodType<
  PersistedSemanticSearchSharedWithEntry
> = z.object({
  accessLevel: PersistedSemanticSearchSharedWithEntryAccessLevel$zodSchema,
  userId: z.string(),
}).describe(
  "Entry inside `sharedWith[]`. The schema sets `_id: false` on these\nsub-docs, so no auto-injected `_id` is present.\n",
);
