/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import {
  AgentListResponse,
  AgentListResponse$zodSchema,
} from "./agentlistresponse.js";
import { ErrorResponse, ErrorResponse$zodSchema } from "./errorresponse.js";

/**
 * Sort direction.
 */
export const ListAgentsSortOrder = {
  Asc: "asc",
  Desc: "desc",
} as const;
/**
 * Sort direction.
 */
export type ListAgentsSortOrder = ClosedEnum<typeof ListAgentsSortOrder>;

export const ListAgentsSortOrder$zodSchema = z.enum([
  "asc",
  "desc",
]).describe("Sort direction.");

export type ListAgentsRequest = {
  page?: number | undefined;
  limit?: number | undefined;
  search?: string | undefined;
  sort_by?: string | undefined;
  sort_order?: ListAgentsSortOrder | undefined;
};

export const ListAgentsRequest$zodSchema: z.ZodType<ListAgentsRequest> = z
  .object({
    limit: z.int().default(20).describe(
      "Maximum number of agents to return in the current page.",
    ),
    page: z.int().default(1).describe("1-based page number."),
    search: z.string().describe(
      "Case-insensitive search across agent name, description, and tags. Leading/trailing whitespace is trimmed; blank-after-trim values are rejected.",
    ).optional(),
    sort_by: z.string().default("updatedAtTimestamp").describe(
      "Backend sort field. Leading/trailing whitespace is trimmed. Common value is `updatedAtTimestamp`.",
    ),
    sort_order: ListAgentsSortOrder$zodSchema.default("desc"),
  });

export type ListAgentsResponse = AgentListResponse | ErrorResponse;

export const ListAgentsResponse$zodSchema: z.ZodType<ListAgentsResponse> = z
  .union([
    AgentListResponse$zodSchema,
    ErrorResponse$zodSchema,
  ]);
