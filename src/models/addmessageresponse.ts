/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Conversation, Conversation$zodSchema } from "./conversation.js";

export type AddMessageResponseMeta = {
  requestId: string;
  timestamp: string;
  duration: number;
};

export const AddMessageResponseMeta$zodSchema: z.ZodType<
  AddMessageResponseMeta
> = z.object({
  duration: z.int(),
  requestId: z.string(),
  timestamp: z.iso.datetime({ offset: true }),
});

/**
 * Response from `POST /conversations/{conversationId}/messages`. Same
 *
 * @remarks
 * `{ conversation, meta }` envelope as `SSECompleteData` plus a
 * `recordsUsed` count for the new turn.
 */
export type AddMessageResponse = {
  conversation: Conversation;
  recordsUsed?: number | undefined;
  meta: AddMessageResponseMeta;
};

export const AddMessageResponse$zodSchema: z.ZodType<AddMessageResponse> = z
  .object({
    conversation: Conversation$zodSchema,
    meta: z.lazy(() => AddMessageResponseMeta$zodSchema),
    recordsUsed: z.int().optional(),
  }).describe(
    "Response from `POST /conversations/{conversationId}/messages`. Same\n`{ conversation, meta }` envelope as `SSECompleteData` plus a\n`recordsUsed` count for the new turn.\n",
  );
