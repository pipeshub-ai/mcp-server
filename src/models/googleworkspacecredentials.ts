/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * Type of credentials: 'individual' for OAuth, 'business' for service account
 */
export const UserType = {
  Individual: "individual",
  Business: "business",
} as const;
/**
 * Type of credentials: 'individual' for OAuth, 'business' for service account
 */
export type UserType = ClosedEnum<typeof UserType>;

export const UserType$zodSchema = z.enum([
  "individual",
  "business",
]).describe(
  "Type of credentials: 'individual' for OAuth, 'business' for service account",
);

/**
 * Credential data (structure depends on userType)
 */
export type GoogleWorkspaceCredentialsCredentials = {};

export const GoogleWorkspaceCredentialsCredentials$zodSchema: z.ZodType<
  GoogleWorkspaceCredentialsCredentials
> = z.object({}).describe("Credential data (structure depends on userType)");

/**
 * Google Workspace service account or OAuth credentials
 */
export type GoogleWorkspaceCredentials = {
  userType?: UserType | undefined;
  credentials?: GoogleWorkspaceCredentialsCredentials | undefined;
};

export const GoogleWorkspaceCredentials$zodSchema: z.ZodType<
  GoogleWorkspaceCredentials
> = z.object({
  credentials: z.lazy(() => GoogleWorkspaceCredentialsCredentials$zodSchema)
    .optional(),
  userType: UserType$zodSchema.optional(),
}).describe("Google Workspace service account or OAuth credentials");
