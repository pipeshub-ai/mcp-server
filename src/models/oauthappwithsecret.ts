/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * App status
 */
export const OAuthAppWithSecretStatus = {
  Active: "active",
  Suspended: "suspended",
  Revoked: "revoked",
} as const;
/**
 * App status
 */
export type OAuthAppWithSecretStatus = ClosedEnum<
  typeof OAuthAppWithSecretStatus
>;

export const OAuthAppWithSecretStatus$zodSchema = z.enum([
  "active",
  "suspended",
  "revoked",
]).describe("App status");

/**
 * OAuth app details (without secret). Fields under `required:` always appear
 *
 * @remarks
 * in `toAppResponse` (`oauth.app.service.ts`); optional URL/description fields
 * are only present when set by the caller.
 */
export type OAuthAppWithSecret = {
  id: string;
  slug: string;
  clientId: string;
  name: string;
  description?: string | undefined;
  redirectUris: Array<string>;
  allowedGrantTypes: Array<string>;
  allowedScopes: Array<string>;
  status: OAuthAppWithSecretStatus;
  homepageUrl?: string | undefined;
  privacyPolicyUrl?: string | undefined;
  termsOfServiceUrl?: string | undefined;
  isConfidential: boolean;
  accessTokenLifetime: number;
  refreshTokenLifetime: number;
  createdAt: string;
  updatedAt: string;
  clientSecret: string;
};

export const OAuthAppWithSecret$zodSchema: z.ZodType<OAuthAppWithSecret> = z
  .object({
    accessTokenLifetime: z.int(),
    allowedGrantTypes: z.array(z.string()),
    allowedScopes: z.array(z.string()),
    clientId: z.string(),
    clientSecret: z.string(),
    createdAt: z.iso.datetime({ offset: true }),
    description: z.string().optional(),
    homepageUrl: z.string().optional(),
    id: z.string(),
    isConfidential: z.boolean(),
    name: z.string(),
    privacyPolicyUrl: z.string().optional(),
    redirectUris: z.array(z.string()),
    refreshTokenLifetime: z.int(),
    slug: z.string(),
    status: OAuthAppWithSecretStatus$zodSchema,
    termsOfServiceUrl: z.string().optional(),
    updatedAt: z.iso.datetime({ offset: true }),
  }).describe(
    "OAuth app details (without secret). Fields under `required:` always appear\nin `toAppResponse` (`oauth.app.service.ts`); optional URL/description fields\nare only present when set by the caller.\n",
  );
