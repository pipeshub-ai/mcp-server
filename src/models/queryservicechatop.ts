/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const QueryServiceChatOpServerList = [
  /**
   * Query Service (Internal)
   */
  "http://localhost:8000",
] as const;

export const QueryServiceChatRole = {
  UserQuery: "user_query",
  BotResponse: "bot_response",
} as const;
export type QueryServiceChatRole = ClosedEnum<typeof QueryServiceChatRole>;

export const QueryServiceChatRole$zodSchema = z.enum([
  "user_query",
  "bot_response",
]);

export type PreviousConversation = {
  role?: QueryServiceChatRole | undefined;
  content?: string | undefined;
};

export const PreviousConversation$zodSchema: z.ZodType<PreviousConversation> = z
  .object({
    content: z.string().optional(),
    role: QueryServiceChatRole$zodSchema.optional(),
  });

export type QueryServiceChatFilters = {};

export const QueryServiceChatFilters$zodSchema: z.ZodType<
  QueryServiceChatFilters
> = z.object({});

export const RetrievalMode = {
  Hybrid: "HYBRID",
  Vector: "VECTOR",
  Keyword: "KEYWORD",
} as const;
export type RetrievalMode = ClosedEnum<typeof RetrievalMode>;

export const RetrievalMode$zodSchema = z.enum([
  "HYBRID",
  "VECTOR",
  "KEYWORD",
]);

export const ChatMode = {
  Quick: "quick",
  Analysis: "analysis",
  DeepResearch: "deep_research",
  Creative: "creative",
  Precise: "precise",
  Standard: "standard",
} as const;
export type ChatMode = ClosedEnum<typeof ChatMode>;

export const ChatMode$zodSchema = z.enum([
  "quick",
  "analysis",
  "deep_research",
  "creative",
  "precise",
  "standard",
]);

/**
 * Request payload
 */
export type QueryServiceChatRequest = {
  query: string;
  limit?: number | undefined;
  previousConversations?: Array<PreviousConversation> | undefined;
  filters?: QueryServiceChatFilters | undefined;
  retrievalMode?: RetrievalMode | undefined;
  quickMode?: boolean | undefined;
  modelKey?: string | undefined;
  modelName?: string | undefined;
  chatMode?: ChatMode | undefined;
};

export const QueryServiceChatRequest$zodSchema: z.ZodType<
  QueryServiceChatRequest
> = z.object({
  chatMode: ChatMode$zodSchema.default("standard"),
  filters: z.lazy(() => QueryServiceChatFilters$zodSchema).optional(),
  limit: z.int().default(50),
  modelKey: z.string().optional(),
  modelName: z.string().optional(),
  previousConversations: z.array(z.lazy(() => PreviousConversation$zodSchema))
    .optional(),
  query: z.string(),
  quickMode: z.boolean().default(false),
  retrievalMode: RetrievalMode$zodSchema.default("HYBRID"),
}).describe("Request payload");
