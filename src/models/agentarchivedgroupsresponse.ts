/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  AgentArchivedConversationGroup,
  AgentArchivedConversationGroup$zodSchema,
} from "./agentarchivedconversationgroup.js";
import { RequestMeta, RequestMeta$zodSchema } from "./requestmeta.js";
import {
  SemanticSearchHistoryPagination,
  SemanticSearchHistoryPagination$zodSchema,
} from "./semanticsearchhistorypagination.js";

/**
 * Response from `GET /agents/conversations/show/archives` — archived agent
 *
 * @remarks
 * conversations grouped by `agentKey`, with agent-level pagination over
 * the groups and a fixed slice of conversations under each agent.
 */
export type AgentArchivedGroupsResponse = {
  groups: Array<AgentArchivedConversationGroup>;
  agentPagination: SemanticSearchHistoryPagination;
  meta: RequestMeta;
};

export const AgentArchivedGroupsResponse$zodSchema: z.ZodType<
  AgentArchivedGroupsResponse
> = z.object({
  agentPagination: SemanticSearchHistoryPagination$zodSchema,
  groups: z.array(AgentArchivedConversationGroup$zodSchema),
  meta: RequestMeta$zodSchema,
}).describe(
  "Response from `GET /agents/conversations/show/archives` — archived agent\nconversations grouped by `agentKey`, with agent-level pagination over\nthe groups and a fixed slice of conversations under each agent.\n",
);
