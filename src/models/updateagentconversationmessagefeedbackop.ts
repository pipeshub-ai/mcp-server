/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  MessageFeedbackSubmitRequest,
  MessageFeedbackSubmitRequest$zodSchema,
} from "./messagefeedbacksubmitrequest.js";

export type UpdateAgentConversationMessageFeedbackRequest = {
  agentKey: string;
  conversationId: string;
  messageId: string;
  body: MessageFeedbackSubmitRequest;
};

export const UpdateAgentConversationMessageFeedbackRequest$zodSchema: z.ZodType<
  UpdateAgentConversationMessageFeedbackRequest
> = z.object({
  agentKey: z.string().describe(
    "Unique agent identifier (gateway Zod requires non-empty string).",
  ),
  body: MessageFeedbackSubmitRequest$zodSchema,
  conversationId: z.string().describe("Unique conversation identifier."),
  messageId: z.string().describe(
    "Identifier of the bot-response message being rated.",
  ),
});
