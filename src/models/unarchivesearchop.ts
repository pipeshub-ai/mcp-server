/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export type UnarchiveSearchRequest = { searchId: string };

export const UnarchiveSearchRequest$zodSchema: z.ZodType<
  UnarchiveSearchRequest
> = z.object({
  searchId: z.string().describe("Unique search identifier"),
});

/**
 * Resulting status of the search after the operation.
 */
export const UnarchiveSearchStatus = {
  Unarchived: "unarchived",
} as const;
/**
 * Resulting status of the search after the operation.
 */
export type UnarchiveSearchStatus = ClosedEnum<typeof UnarchiveSearchStatus>;

export const UnarchiveSearchStatus$zodSchema = z.enum([
  "unarchived",
]).describe("Resulting status of the search after the operation.");

export type UnarchiveSearchMeta = {
  requestId?: string | undefined;
  timestamp: string;
  duration: number;
};

export const UnarchiveSearchMeta$zodSchema: z.ZodType<UnarchiveSearchMeta> = z
  .object({
    duration: z.int(),
    requestId: z.string().optional(),
    timestamp: z.iso.datetime({ offset: true }),
  });

/**
 * Search unarchived successfully
 */
export type UnarchiveSearchResponse = {
  id: string;
  status: UnarchiveSearchStatus;
  unarchivedBy: string;
  unarchivedAt: string;
  meta: UnarchiveSearchMeta;
};

export const UnarchiveSearchResponse$zodSchema: z.ZodType<
  UnarchiveSearchResponse
> = z.object({
  id: z.string(),
  meta: z.lazy(() => UnarchiveSearchMeta$zodSchema),
  status: UnarchiveSearchStatus$zodSchema,
  unarchivedAt: z.iso.datetime({ offset: true }),
  unarchivedBy: z.string(),
}).describe("Search unarchived successfully");
