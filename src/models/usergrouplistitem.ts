/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const UserGroupListItemType = {
  Admin: "admin",
  Standard: "standard",
  Everyone: "everyone",
  Custom: "custom",
} as const;
export type UserGroupListItemType = ClosedEnum<typeof UserGroupListItemType>;

export const UserGroupListItemType$zodSchema = z.enum([
  "admin",
  "standard",
  "everyone",
  "custom",
]);

/**
 * Single user group as returned by `GET /userGroups`. Mirrors the
 *
 * @remarks
 * persisted document plus a denormalized `userCount`. The compact
 * embedded form used inside user list responses is `UserGroupRef`;
 * the canonical full document is `UserGroup`.
 */
export type UserGroupListItem = {
  _id?: string | undefined;
  name?: string | undefined;
  type?: UserGroupListItemType | undefined;
  orgId?: string | undefined;
  slug?: string | undefined;
  isDeleted?: boolean | undefined;
  userCount?: number | undefined;
  createdAt?: string | undefined;
  updatedAt?: string | undefined;
};

export const UserGroupListItem$zodSchema: z.ZodType<UserGroupListItem> = z
  .object({
    _id: z.string().optional(),
    createdAt: z.iso.datetime({ offset: true }).optional(),
    isDeleted: z.boolean().default(false),
    name: z.string().optional(),
    orgId: z.string().optional(),
    slug: z.string().optional(),
    type: UserGroupListItemType$zodSchema.optional(),
    updatedAt: z.iso.datetime({ offset: true }).optional(),
    userCount: z.int().optional(),
  }).describe(
    "Single user group as returned by `GET /userGroups`. Mirrors the\npersisted document plus a denormalized `userCount`. The compact\nembedded form used inside user list responses is `UserGroupRef`;\nthe canonical full document is `UserGroup`.\n",
  );
