/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

export const CreateOAuthAppRequestAllowedGrantType = {
  AuthorizationCode: "authorization_code",
  ClientCredentials: "client_credentials",
  RefreshToken: "refresh_token",
} as const;
export type CreateOAuthAppRequestAllowedGrantType = ClosedEnum<
  typeof CreateOAuthAppRequestAllowedGrantType
>;

export const CreateOAuthAppRequestAllowedGrantType$zodSchema = z.enum([
  "authorization_code",
  "client_credentials",
  "refresh_token",
]);

/**
 * Request to create a new OAuth app (`createAppSchema` in `oauth.validators.ts`).
 *
 * @remarks
 *
 * **Required:** `name`, `allowedScopes`. **Optional:** `description`, `redirectUris`, `allowedGrantTypes`,
 * `homepageUrl`, `privacyPolicyUrl`, `termsOfServiceUrl`, `isConfidential`, `accessTokenLifetime`, `refreshTokenLifetime`.
 *
 * **Redirect rule (Zod refine):** If the effective grant list includes `authorization_code`
 * (including when `allowedGrantTypes` is omitted — defaults to `authorization_code` + `refresh_token`),
 * at least one redirect URI is required. If grants exclude `authorization_code`, `redirectUris` may be omitted.
 */
export type CreateOAuthAppRequest = {
  name: string;
  description?: string | undefined;
  redirectUris?: Array<string> | undefined;
  allowedGrantTypes?: Array<CreateOAuthAppRequestAllowedGrantType> | undefined;
  allowedScopes: Array<string>;
  homepageUrl?: string | undefined;
  privacyPolicyUrl?: string | undefined;
  termsOfServiceUrl?: string | undefined;
  isConfidential?: boolean | undefined;
  accessTokenLifetime?: number | undefined;
  refreshTokenLifetime?: number | undefined;
};

export const CreateOAuthAppRequest$zodSchema: z.ZodType<CreateOAuthAppRequest> =
  z.object({
    accessTokenLifetime: z.int().default(3600),
    allowedGrantTypes: z.array(CreateOAuthAppRequestAllowedGrantType$zodSchema)
      .optional(),
    allowedScopes: z.array(z.string()),
    description: z.string().optional(),
    homepageUrl: z.string().optional(),
    isConfidential: z.boolean().default(true),
    name: z.string(),
    privacyPolicyUrl: z.string().optional(),
    redirectUris: z.array(z.string()).optional(),
    refreshTokenLifetime: z.int().default(2592000),
    termsOfServiceUrl: z.string().optional(),
  }).describe(
    "Request to create a new OAuth app (`createAppSchema` in `oauth.validators.ts`).\n\n**Required:** `name`, `allowedScopes`. **Optional:** `description`, `redirectUris`, `allowedGrantTypes`,\n`homepageUrl`, `privacyPolicyUrl`, `termsOfServiceUrl`, `isConfidential`, `accessTokenLifetime`, `refreshTokenLifetime`.\n\n**Redirect rule (Zod refine):** If the effective grant list includes `authorization_code`\n(including when `allowedGrantTypes` is omitted — defaults to `authorization_code` + `refresh_token`),\nat least one redirect URI is required. If grants exclude `authorization_code`, `redirectUris` may be omitted.\n",
  );
