/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  AgentConversationListItem,
  AgentConversationListItem$zodSchema,
} from "./agentconversationlistitem.js";
import {
  SemanticSearchHistoryPagination,
  SemanticSearchHistoryPagination$zodSchema,
} from "./semanticsearchhistorypagination.js";

/**
 * Archived conversations for a single agent, sliced to the first page
 *
 * @remarks
 * of the per-agent archive query (limit 5, sorted newest first).
 */
export type AgentArchivedConversationGroup = {
  agentKey: string;
  conversations: Array<AgentConversationListItem>;
  pagination: SemanticSearchHistoryPagination;
};

export const AgentArchivedConversationGroup$zodSchema: z.ZodType<
  AgentArchivedConversationGroup
> = z.object({
  agentKey: z.string(),
  conversations: z.array(AgentConversationListItem$zodSchema),
  pagination: SemanticSearchHistoryPagination$zodSchema,
}).describe(
  "Archived conversations for a single agent, sliced to the first page\nof the per-agent archive query (limit 5, sorted newest first).\n",
);
