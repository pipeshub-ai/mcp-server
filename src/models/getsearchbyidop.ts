/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import {
  PersistedSemanticSearch,
  PersistedSemanticSearch$zodSchema,
} from "./persistedsemanticsearch.js";

export type GetSearchByIdRequest = { searchId: string };

export const GetSearchByIdRequest$zodSchema: z.ZodType<GetSearchByIdRequest> = z
  .object({
    searchId: z.string().describe("Unique search identifier"),
  });

/**
 * Machine-readable error code.
 *
 * @remarks
 *
 * - `HTTP_INTERNAL_SERVER_ERROR` — explicit
 *   `InternalServerError` raised by the handler.
 * - `INTERNAL_ERROR` — unhandled exception
 *   caught by the global error middleware.
 * - `MIDDLEWARE_ERROR` — the error middleware
 *   itself failed while serializing the
 *   response.
 */
export const InternalServerErrorErrorCode = {
  HttpInternalServerError: "HTTP_INTERNAL_SERVER_ERROR",
  InternalError: "INTERNAL_ERROR",
  MiddlewareError: "MIDDLEWARE_ERROR",
} as const;
/**
 * Machine-readable error code.
 *
 * @remarks
 *
 * - `HTTP_INTERNAL_SERVER_ERROR` — explicit
 *   `InternalServerError` raised by the handler.
 * - `INTERNAL_ERROR` — unhandled exception
 *   caught by the global error middleware.
 * - `MIDDLEWARE_ERROR` — the error middleware
 *   itself failed while serializing the
 *   response.
 */
export type InternalServerErrorErrorCode = ClosedEnum<
  typeof InternalServerErrorErrorCode
>;

export const InternalServerErrorErrorCode$zodSchema = z.enum([
  "HTTP_INTERNAL_SERVER_ERROR",
  "INTERNAL_ERROR",
  "MIDDLEWARE_ERROR",
]).describe(
  "Machine-readable error code.\n\n- `HTTP_INTERNAL_SERVER_ERROR` — explicit\n  `InternalServerError` raised by the handler.\n- `INTERNAL_ERROR` — unhandled exception\n  caught by the global error middleware.\n- `MIDDLEWARE_ERROR` — the error middleware\n  itself failed while serializing the\n  response.\n",
);

export type GetSearchByIdInternalServerErrorError = {
  code: InternalServerErrorErrorCode;
  message: string;
};

export const GetSearchByIdInternalServerErrorError$zodSchema: z.ZodType<
  GetSearchByIdInternalServerErrorError
> = z.object({
  code: InternalServerErrorErrorCode$zodSchema,
  message: z.string(),
});

/**
 * Server error. Possible causes:
 *
 * @remarks
 *
 * - Explicit `InternalServerError`
 *   or any other 500 `BaseError` thrown by the handler.
 * - Non-`BaseError` exception caught by the
 *   global error middleware.
 * - Response serializer fallback.
 */
export type GetSearchByIdInternalServerErrorResponseBody = {
  error: GetSearchByIdInternalServerErrorError;
};

export const GetSearchByIdInternalServerErrorResponseBody$zodSchema: z.ZodType<
  GetSearchByIdInternalServerErrorResponseBody
> = z.object({
  error: z.lazy(() => GetSearchByIdInternalServerErrorError$zodSchema),
}).describe(
  "Server error. Possible causes:\n\n- Explicit `InternalServerError`\n  or any other 500 `BaseError` thrown by the handler.\n- Non-`BaseError` exception caught by the\n  global error middleware.\n- Response serializer fallback.\n",
);

/**
 * Machine-readable error code. `HTTP_NOT_FOUND`
 *
 * @remarks
 * is emitted when the addressed resource does
 * not exist.
 */
export const GetSearchByIdNotFoundCode = {
  HttpNotFound: "HTTP_NOT_FOUND",
} as const;
/**
 * Machine-readable error code. `HTTP_NOT_FOUND`
 *
 * @remarks
 * is emitted when the addressed resource does
 * not exist.
 */
export type GetSearchByIdNotFoundCode = ClosedEnum<
  typeof GetSearchByIdNotFoundCode
>;

export const GetSearchByIdNotFoundCode$zodSchema = z.enum([
  "HTTP_NOT_FOUND",
]).describe(
  "Machine-readable error code. `HTTP_NOT_FOUND`\nis emitted when the addressed resource does\nnot exist.\n",
);

export type GetSearchByIdNotFoundError = {
  code: GetSearchByIdNotFoundCode;
  message: string;
};

export const GetSearchByIdNotFoundError$zodSchema: z.ZodType<
  GetSearchByIdNotFoundError
> = z.object({
  code: GetSearchByIdNotFoundCode$zodSchema,
  message: z.string(),
});

/**
 * Reserved for parity with sibling routes; this endpoint
 *
 * @remarks
 * currently returns `200` with an empty array for an unknown
 * id rather than emitting `404`.
 */
export type GetSearchByIdNotFoundResponseBody = {
  error: GetSearchByIdNotFoundError;
};

export const GetSearchByIdNotFoundResponseBody$zodSchema: z.ZodType<
  GetSearchByIdNotFoundResponseBody
> = z.object({
  error: z.lazy(() => GetSearchByIdNotFoundError$zodSchema),
}).describe(
  "Reserved for parity with sibling routes; this endpoint\ncurrently returns `200` with an empty array for an unknown\nid rather than emitting `404`.\n",
);

/**
 * Machine-readable error code. `HTTP_FORBIDDEN`
 *
 * @remarks
 * is emitted when the bearer token is valid but
 * lacks the required scope.
 */
export const GetSearchByIdForbiddenCode = {
  HttpForbidden: "HTTP_FORBIDDEN",
} as const;
/**
 * Machine-readable error code. `HTTP_FORBIDDEN`
 *
 * @remarks
 * is emitted when the bearer token is valid but
 * lacks the required scope.
 */
export type GetSearchByIdForbiddenCode = ClosedEnum<
  typeof GetSearchByIdForbiddenCode
>;

export const GetSearchByIdForbiddenCode$zodSchema = z.enum([
  "HTTP_FORBIDDEN",
]).describe(
  "Machine-readable error code. `HTTP_FORBIDDEN`\nis emitted when the bearer token is valid but\nlacks the required scope.\n",
);

export type GetSearchByIdErrorHTTPForbidden = {
  code: GetSearchByIdForbiddenCode;
  message: string;
};

export const GetSearchByIdErrorHTTPForbidden$zodSchema: z.ZodType<
  GetSearchByIdErrorHTTPForbidden
> = z.object({
  code: GetSearchByIdForbiddenCode$zodSchema,
  message: z.string(),
});

/**
 * Bearer token lacks the `semantic:read` scope.
 *
 * @remarks
 */
export type GetSearchByIdForbiddenResponseBody = {
  error: GetSearchByIdErrorHTTPForbidden;
};

export const GetSearchByIdForbiddenResponseBody$zodSchema: z.ZodType<
  GetSearchByIdForbiddenResponseBody
> = z.object({
  error: z.lazy(() => GetSearchByIdErrorHTTPForbidden$zodSchema),
}).describe("Bearer token lacks the `semantic:read` scope.\n");

/**
 * Machine-readable error code. `HTTP_UNAUTHORIZED`
 *
 * @remarks
 * is emitted when the bearer token is missing,
 * invalid, or expired.
 */
export const GetSearchByIdUnauthorizedCode = {
  HttpUnauthorized: "HTTP_UNAUTHORIZED",
} as const;
/**
 * Machine-readable error code. `HTTP_UNAUTHORIZED`
 *
 * @remarks
 * is emitted when the bearer token is missing,
 * invalid, or expired.
 */
export type GetSearchByIdUnauthorizedCode = ClosedEnum<
  typeof GetSearchByIdUnauthorizedCode
>;

export const GetSearchByIdUnauthorizedCode$zodSchema = z.enum([
  "HTTP_UNAUTHORIZED",
]).describe(
  "Machine-readable error code. `HTTP_UNAUTHORIZED`\nis emitted when the bearer token is missing,\ninvalid, or expired.\n",
);

export type GetSearchByIdErrorHTTPUnauthorized = {
  code: GetSearchByIdUnauthorizedCode;
  message: string;
};

export const GetSearchByIdErrorHTTPUnauthorized$zodSchema: z.ZodType<
  GetSearchByIdErrorHTTPUnauthorized
> = z.object({
  code: GetSearchByIdUnauthorizedCode$zodSchema,
  message: z.string(),
});

/**
 * Missing or invalid bearer token.
 *
 * @remarks
 */
export type GetSearchByIdUnauthorizedResponseBody = {
  error: GetSearchByIdErrorHTTPUnauthorized;
};

export const GetSearchByIdUnauthorizedResponseBody$zodSchema: z.ZodType<
  GetSearchByIdUnauthorizedResponseBody
> = z.object({
  error: z.lazy(() => GetSearchByIdErrorHTTPUnauthorized$zodSchema),
}).describe("Missing or invalid bearer token.\n");

/**
 * Machine-readable error code. `VALIDATION_ERROR`
 *
 * @remarks
 * is emitted when the request fails Zod
 * validation.
 */
export const GetSearchByIdCodeValidationError = {
  ValidationError: "VALIDATION_ERROR",
} as const;
/**
 * Machine-readable error code. `VALIDATION_ERROR`
 *
 * @remarks
 * is emitted when the request fails Zod
 * validation.
 */
export type GetSearchByIdCodeValidationError = ClosedEnum<
  typeof GetSearchByIdCodeValidationError
>;

export const GetSearchByIdCodeValidationError$zodSchema = z.enum([
  "VALIDATION_ERROR",
]).describe(
  "Machine-readable error code. `VALIDATION_ERROR`\nis emitted when the request fails Zod\nvalidation.\n",
);

export type GetSearchByIdErrorValidationError = {
  code: GetSearchByIdCodeValidationError;
  message: string;
};

export const GetSearchByIdErrorValidationError$zodSchema: z.ZodType<
  GetSearchByIdErrorValidationError
> = z.object({
  code: GetSearchByIdCodeValidationError$zodSchema,
  message: z.string(),
});

/**
 * Invalid request — `searchId`
 *
 * @remarks
 * failed Zod validation (not a valid ObjectId).
 */
export type GetSearchByIdBadRequestResponseBody = {
  error: GetSearchByIdErrorValidationError;
};

export const GetSearchByIdBadRequestResponseBody$zodSchema: z.ZodType<
  GetSearchByIdBadRequestResponseBody
> = z.object({
  error: z.lazy(() => GetSearchByIdErrorValidationError$zodSchema),
}).describe(
  "Invalid request — `searchId`\nfailed Zod validation (not a valid ObjectId).\n",
);

export type GetSearchByIdResponse =
  | GetSearchByIdBadRequestResponseBody
  | GetSearchByIdUnauthorizedResponseBody
  | GetSearchByIdForbiddenResponseBody
  | GetSearchByIdNotFoundResponseBody
  | GetSearchByIdInternalServerErrorResponseBody
  | Array<PersistedSemanticSearch>;

export const GetSearchByIdResponse$zodSchema: z.ZodType<GetSearchByIdResponse> =
  z.union([
    z.lazy(() => GetSearchByIdBadRequestResponseBody$zodSchema),
    z.lazy(() => GetSearchByIdUnauthorizedResponseBody$zodSchema),
    z.lazy(() => GetSearchByIdForbiddenResponseBody$zodSchema),
    z.lazy(() => GetSearchByIdNotFoundResponseBody$zodSchema),
    z.lazy(() => GetSearchByIdInternalServerErrorResponseBody$zodSchema),
    z.array(PersistedSemanticSearch$zodSchema).describe(
      "Array containing zero or one persisted search document.",
    ),
  ]);
