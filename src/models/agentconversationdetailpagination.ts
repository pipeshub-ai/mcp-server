/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type MessageRange = { start: number; end: number };

export const MessageRange$zodSchema: z.ZodType<MessageRange> = z.object({
  end: z.int(),
  start: z.int(),
});

/**
 * Message pagination returned inside the `conversation` object. The
 *
 * @remarks
 * handler paginates backwards from the end of the stored message array,
 * then sorts the selected page in memory before serialization.
 */
export type AgentConversationDetailPagination = {
  page: number;
  limit: number;
  totalCount: number;
  totalPages: number;
  hasNextPage: boolean;
  hasPrevPage: boolean;
  messageRange: MessageRange;
};

export const AgentConversationDetailPagination$zodSchema: z.ZodType<
  AgentConversationDetailPagination
> = z.object({
  hasNextPage: z.boolean(),
  hasPrevPage: z.boolean(),
  limit: z.int(),
  messageRange: z.lazy(() => MessageRange$zodSchema),
  page: z.int(),
  totalCount: z.int(),
  totalPages: z.int(),
}).describe(
  "Message pagination returned inside the `conversation` object. The\nhandler paginates backwards from the end of the stored message array,\nthen sorts the selected page in memory before serialization.\n",
);
