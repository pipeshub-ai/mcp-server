/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import {
  ApplicationJsonErrorResponse,
  ApplicationJsonErrorResponse$zodSchema,
} from "./applicationjsonerrorresponse.js";
import {
  OAuthAppListResponse,
  OAuthAppListResponse$zodSchema,
} from "./oauthapplistresponse.js";
import {
  OAuthClientManagementRateLimitError,
  OAuthClientManagementRateLimitError$zodSchema,
} from "./oauthclientmanagementratelimiterror.js";

/**
 * Filter by status
 */
export const ListOAuthAppsStatus = {
  Active: "active",
  Suspended: "suspended",
  Revoked: "revoked",
} as const;
/**
 * Filter by status
 */
export type ListOAuthAppsStatus = ClosedEnum<typeof ListOAuthAppsStatus>;

export const ListOAuthAppsStatus$zodSchema = z.enum([
  "active",
  "suspended",
  "revoked",
]).describe("Filter by status");

export type ListOAuthAppsRequest = {
  page?: number | undefined;
  limit?: number | undefined;
  status?: ListOAuthAppsStatus | undefined;
  search?: string | undefined;
};

export const ListOAuthAppsRequest$zodSchema: z.ZodType<ListOAuthAppsRequest> = z
  .object({
    limit: z.int().default(20).describe(
      "Items per page (defaults to `20` when omitted or empty; max 100).\n",
    ),
    page: z.int().default(1).describe(
      "Page number (matches `listAppsQuerySchema`: defaults to `1` when omitted or empty).\n",
    ),
    search: z.string().describe(
      "Search by app name or description (case-insensitive)",
    ).optional(),
    status: ListOAuthAppsStatus$zodSchema.optional(),
  });

export type ListOAuthAppsResponse =
  | OAuthAppListResponse
  | ApplicationJsonErrorResponse
  | OAuthClientManagementRateLimitError;

export const ListOAuthAppsResponse$zodSchema: z.ZodType<ListOAuthAppsResponse> =
  z.union([
    OAuthAppListResponse$zodSchema,
    ApplicationJsonErrorResponse$zodSchema,
    OAuthClientManagementRateLimitError$zodSchema,
  ]);
