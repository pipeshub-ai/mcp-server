/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";
import {
  StoredAgentConversation,
  StoredAgentConversation$zodSchema,
} from "./storedagentconversation.js";

export const MessageEnum = {
  ConversationDeletedSuccessfully: "Conversation deleted successfully",
} as const;
export type MessageEnum = ClosedEnum<typeof MessageEnum>;

export const MessageEnum$zodSchema = z.enum([
  "Conversation deleted successfully",
]);

/**
 * Envelope returned by `DELETE /agents/{agentKey}/conversations/{conversationId}`.
 *
 * @remarks
 * When the conversation does not exist, belongs to a different agent, or
 * was already deleted, the API still returns HTTP 200 with
 * `conversation: null`.
 */
export type AgentConversationDeleteResponse = {
  message: MessageEnum;
  conversation: StoredAgentConversation;
};

export const AgentConversationDeleteResponse$zodSchema: z.ZodType<
  AgentConversationDeleteResponse
> = z.object({
  conversation: StoredAgentConversation$zodSchema,
  message: MessageEnum$zodSchema,
}).describe(
  "Envelope returned by `DELETE /agents/{agentKey}/conversations/{conversationId}`.\nWhen the conversation does not exist, belongs to a different agent, or\nwas already deleted, the API still returns HTTP 200 with\n`conversation: null`.\n",
);
