/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../types/enums.js";

/**
 * Type of token
 */
export const TokenType = {
  Access: "access",
  Refresh: "refresh",
} as const;
/**
 * Type of token
 */
export type TokenType = ClosedEnum<typeof TokenType>;

export const TokenType$zodSchema = z.enum([
  "access",
  "refresh",
]).describe("Type of token");

/**
 * Information about an issued token (one element returned by `listTokensForApp`
 *
 * @remarks
 * in `oauth_token.service.ts`). `userId` is omitted for client-credentials access
 * tokens; all other fields are always populated.
 */
export type OAuthTokenListItem = {
  id: string;
  tokenType: TokenType;
  userId?: string | undefined;
  scopes: Array<string>;
  createdAt: string;
  expiresAt: string;
  isRevoked: boolean;
};

export const OAuthTokenListItem$zodSchema: z.ZodType<OAuthTokenListItem> = z
  .object({
    createdAt: z.iso.datetime({ offset: true }),
    expiresAt: z.iso.datetime({ offset: true }),
    id: z.string(),
    isRevoked: z.boolean(),
    scopes: z.array(z.string()),
    tokenType: TokenType$zodSchema,
    userId: z.string().optional(),
  }).describe(
    "Information about an issued token (one element returned by `listTokensForApp`\nin `oauth_token.service.ts`). `userId` is omitted for client-credentials access\ntokens; all other fields are always populated.\n",
  );
