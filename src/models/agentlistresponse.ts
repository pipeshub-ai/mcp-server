/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { AgentListItem, AgentListItem$zodSchema } from "./agentlistitem.js";
import {
  AgentListPagination,
  AgentListPagination$zodSchema,
} from "./agentlistpagination.js";

/**
 * Paginated response returned by `GET /agents`.
 *
 * @remarks
 *
 * The Node gateway forwards the Python backend response on success. If
 * the backend returns a non-200 response, the gateway still returns HTTP
 * 200 with `success: true`, an empty `agents` array, and a zeroed
 * pagination block derived from the requested `page` / `limit`.
 */
export type AgentListResponse = {
  success: boolean;
  agents: Array<AgentListItem>;
  pagination: AgentListPagination;
};

export const AgentListResponse$zodSchema: z.ZodType<AgentListResponse> = z
  .object({
    agents: z.array(AgentListItem$zodSchema),
    pagination: AgentListPagination$zodSchema,
    success: z.boolean(),
  }).describe(
    "Paginated response returned by `GET /agents`.\n\nThe Node gateway forwards the Python backend response on success. If\nthe backend returns a non-200 response, the gateway still returns HTTP\n200 with `success: true`, an empty `agents` array, and a zeroed\npagination block derived from the requested `page` / `limit`.\n",
  );
