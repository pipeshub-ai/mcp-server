/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Alternative pagination block used by a handful of older endpoints
 *
 * @remarks
 * (e.g. `GET /teams/user/teams`). Same semantics as `PaginationInfo`
 * but with shorter field names: `total`, `pages`, `hasNext`,
 * `hasPrev`. New endpoints should prefer `PaginationInfo`.
 */
export type CompactPaginationInfo = {
  page?: number | undefined;
  limit?: number | undefined;
  total?: number | undefined;
  pages?: number | undefined;
  hasNext?: boolean | undefined;
  hasPrev?: boolean | undefined;
};

export const CompactPaginationInfo$zodSchema: z.ZodType<CompactPaginationInfo> =
  z.object({
    hasNext: z.boolean().optional(),
    hasPrev: z.boolean().optional(),
    limit: z.int().optional(),
    page: z.int().optional(),
    pages: z.int().optional(),
    total: z.int().optional(),
  }).describe(
    "Alternative pagination block used by a handful of older endpoints\n(e.g. `GET /teams/user/teams`). Same semantics as `PaginationInfo`\nbut with shorter field names: `total`, `pages`, `hasNext`,\n`hasPrev`. New endpoints should prefer `PaginationInfo`.\n",
  );
