/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  AppliedFilterNode,
  AppliedFilterNode$zodSchema,
} from "./appliedfilternode.js";

/**
 * Rich filter state selected by the user, used for display and persistence only.
 *
 * @remarks
 * This mirrors the active selection shown in the UI and is distinct from the
 * machine-readable `filters` field used for retrieval scoping.
 */
export type AppliedFilters = {
  apps?: Array<AppliedFilterNode> | undefined;
  kb?: Array<AppliedFilterNode> | undefined;
};

export const AppliedFilters$zodSchema: z.ZodType<AppliedFilters> = z.object({
  apps: z.array(AppliedFilterNode$zodSchema).optional(),
  kb: z.array(AppliedFilterNode$zodSchema).optional(),
}).describe(
  "Rich filter state selected by the user, used for display and persistence only.\nThis mirrors the active selection shown in the UI and is distinct from the\nmachine-readable `filters` field used for retrieval scoping.\n",
);
