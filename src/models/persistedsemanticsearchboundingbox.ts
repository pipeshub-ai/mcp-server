/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Bounding box subdocument embedded in persisted citation metadata.
 *
 * @remarks
 * `boundingBoxSchema` does not set `_id: false`, so Mongoose auto-injects an `_id`.
 */
export type PersistedSemanticSearchBoundingBox = {
  _id: string;
  x: number;
  y: number;
};

export const PersistedSemanticSearchBoundingBox$zodSchema: z.ZodType<
  PersistedSemanticSearchBoundingBox
> = z.object({
  _id: z.string(),
  x: z.number(),
  y: z.number(),
}).describe(
  "Bounding box subdocument embedded in persisted citation metadata.\n`boundingBoxSchema` does not set `_id: false`, so Mongoose auto-injects an `_id`.\n",
);
