/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  AgentConversationListItem,
  AgentConversationListItem$zodSchema,
} from "./agentconversationlistitem.js";
import {
  SemanticSearchHistoryFilters,
  SemanticSearchHistoryFilters$zodSchema,
} from "./semanticsearchhistoryfilters.js";
import {
  SemanticSearchHistoryMeta,
  SemanticSearchHistoryMeta$zodSchema,
} from "./semanticsearchhistorymeta.js";
import {
  SemanticSearchHistoryPagination,
  SemanticSearchHistoryPagination$zodSchema,
} from "./semanticsearchhistorypagination.js";

/**
 * Envelope returned by `GET /agents/{agentKey}/conversations`.
 *
 * @remarks
 * `conversations` contains rows owned by the caller for the agent;
 * `sharedWithMeConversations` contains rows shared with the caller for
 * the same agent. Both arrays use the same pagination and sort inputs,
 * but `pagination.totalCount` and `totalPages` are computed only from
 * `conversations` because the handler counts the owned-query filter only.
 */
export type AgentConversationListResponse = {
  conversations: Array<AgentConversationListItem>;
  sharedWithMeConversations: Array<AgentConversationListItem>;
  pagination: SemanticSearchHistoryPagination;
  filters: SemanticSearchHistoryFilters;
  meta: SemanticSearchHistoryMeta;
};

export const AgentConversationListResponse$zodSchema: z.ZodType<
  AgentConversationListResponse
> = z.object({
  conversations: z.array(AgentConversationListItem$zodSchema),
  filters: SemanticSearchHistoryFilters$zodSchema,
  meta: SemanticSearchHistoryMeta$zodSchema,
  pagination: SemanticSearchHistoryPagination$zodSchema,
  sharedWithMeConversations: z.array(AgentConversationListItem$zodSchema),
}).describe(
  "Envelope returned by `GET /agents/{agentKey}/conversations`.\n`conversations` contains rows owned by the caller for the agent;\n`sharedWithMeConversations` contains rows shared with the caller for\nthe same agent. Both arrays use the same pagination and sort inputs,\nbut `pagination.totalCount` and `totalPages` are computed only from\n`conversations` because the handler counts the owned-query filter only.\n",
);
