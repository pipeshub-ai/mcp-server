/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Request-scoped metadata returned by the archive route. `requestId` is
 *
 * @remarks
 * omitted when upstream middleware did not attach one.
 */
export type AgentConversationArchiveMeta = {
  requestId?: string | undefined;
  timestamp: string;
  duration: number;
};

export const AgentConversationArchiveMeta$zodSchema: z.ZodType<
  AgentConversationArchiveMeta
> = z.object({
  duration: z.int(),
  requestId: z.string().optional(),
  timestamp: z.iso.datetime({ offset: true }),
}).describe(
  "Request-scoped metadata returned by the archive route. `requestId` is\nomitted when upstream middleware did not attach one.\n",
);
