/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type ErrorResponseError = {
  code: string;
  message: string;
  metadata?: { [k: string]: any } | undefined;
};

export const ErrorResponseError$zodSchema: z.ZodType<ErrorResponseError> = z
  .object({
    code: z.string(),
    message: z.string(),
    metadata: z.record(z.string(), z.any()).optional(),
  });

/**
 * Standard error envelope returned by all errors routed through `ErrorMiddleware`.
 *
 * @remarks
 * Applies to all `BaseError` subclasses including `HttpError`, `ValidationError`, and others.
 * The `code` field is a machine-readable string identifying the error type (e.g.
 * `HTTP_UNAUTHORIZED`, `HTTP_NOT_FOUND`, `VALIDATION_ERROR`, `INTERNAL_ERROR`).
 */
export type ErrorResponse = { error: ErrorResponseError };

export const ErrorResponse$zodSchema: z.ZodType<ErrorResponse> = z.object({
  error: z.lazy(() => ErrorResponseError$zodSchema),
}).describe(
  "Standard error envelope returned by all errors routed through `ErrorMiddleware`.\nApplies to all `BaseError` subclasses including `HttpError`, `ValidationError`, and others.\nThe `code` field is a machine-readable string identifying the error type (e.g.\n`HTTP_UNAUTHORIZED`, `HTTP_NOT_FOUND`, `VALIDATION_ERROR`, `INTERNAL_ERROR`).\n",
);
