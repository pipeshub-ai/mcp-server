/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ModelType, ModelType$zodSchema } from "./modeltype.js";

/**
 * A configured AI model the org can use. Returned by
 *
 * @remarks
 * `GET /configurationManager/ai-models/available/{modelType}`.
 * Pass `modelKey` (and optionally `modelName` / `modelFriendlyName`)
 * on chat requests to select this model for the turn.
 */
export type AvailableModel = {
  modelType: ModelType;
  provider: string;
  modelName: string;
  modelKey: string;
  isMultimodal?: boolean | undefined;
  isReasoning?: boolean | undefined;
  isDefault?: boolean | undefined;
};

export const AvailableModel$zodSchema: z.ZodType<AvailableModel> = z.object({
  isDefault: z.boolean().optional(),
  isMultimodal: z.boolean().optional(),
  isReasoning: z.boolean().optional(),
  modelKey: z.string(),
  modelName: z.string(),
  modelType: ModelType$zodSchema,
  provider: z.string(),
}).describe(
  "A configured AI model the org can use. Returned by\n`GET /configurationManager/ai-models/available/{modelType}`.\nPass `modelKey` (and optionally `modelName` / `modelFriendlyName`)\non chat requests to select this model for the turn.\n",
);
