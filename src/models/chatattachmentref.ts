/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * Reference to an attachment produced by `POST /conversations/attachments/upload`
 *
 * @remarks
 * (or the equivalent agent route). Include in create/stream/message bodies
 * so the turn is sent with uploaded files.
 */
export type ChatAttachmentRef = {
  recordId: string;
  recordName?: string | undefined;
  mimeType?: string | undefined;
  extension?: string | undefined;
  virtualRecordId?: string | undefined;
};

export const ChatAttachmentRef$zodSchema: z.ZodType<ChatAttachmentRef> = z
  .object({
    extension: z.string().optional(),
    mimeType: z.string().optional(),
    recordId: z.string(),
    recordName: z.string().optional(),
    virtualRecordId: z.string().optional(),
  }).describe(
    "Reference to an attachment produced by `POST /conversations/attachments/upload`\n(or the equivalent agent route). Include in create/stream/message bodies\nso the turn is sent with uploaded files.\n",
  );
