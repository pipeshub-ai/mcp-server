/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { metricsCollectionSetMetricsPushInterval } from "../../funcs/metricsCollectionSetMetricsPushInterval.js";
import { SetMetricsPushIntervalRequest$zodSchema } from "../../models/setmetricspushintervalop.js";
import { formatResult } from "../tools.js";
const args = {
    request: SetMetricsPushIntervalRequest$zodSchema,
};
export const tool$metricsCollectionSetMetricsPushInterval = {
    name: "metrics-collection-set-metrics-push-interval",
    description: `Configure metrics push interval

Set how frequently collected metrics are pushed to the remote server.

**Interval Guidelines:**
- Minimum: 1000ms (1 second) - for real-time monitoring
- Recommended: 60000ms (1 minute) - balanced performance
- Maximum: No hard limit, but longer intervals may delay insights

**Performance Considerations:**
- Shorter intervals provide more real-time data but increase network traffic
- Longer intervals reduce overhead but delay metric visibility
- Changes take effect on the next push cycle

**Admin Access Required:** This endpoint requires administrator privileges.
`,
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await metricsCollectionSetMetricsPushInterval(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=metricsCollectionSetMetricsPushInterval.js.map