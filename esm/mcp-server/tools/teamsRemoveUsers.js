/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { teamsRemoveUsers } from "../../funcs/teamsRemoveUsers.js";
import { RemoveUsersFromTeamRequest$zodSchema } from "../../models/removeusersfromteamop.js";
import { formatResult } from "../tools.js";
const args = {
    request: RemoveUsersFromTeamRequest$zodSchema,
};
export const tool$teamsRemoveUsers = {
    name: "teams-remove-users",
    description: `Remove users from team

Remove one or more users from a team.<br><br>
<b>Behavior:</b><br>
<ul>
<li>Users not in the team are silently skipped</li>
<li>Removed users lose access to team resources immediately</li>
</ul>
<b>Restrictions:</b><br>
<ul>
<li>Cannot remove the team owner</li>
<li>Only team owner/admin can remove members</li>
<li>Admins cannot remove other admins (only owner can)</li>
</ul>
`,
    annotations: {
        "title": "",
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await teamsRemoveUsers(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=teamsRemoveUsers.js.map