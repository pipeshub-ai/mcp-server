/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { teamsAddUsersToTeam } from "../../funcs/teamsAddUsersToTeam.js";
import { AddUsersToTeamRequest$zodSchema } from "../../models/adduserstoteamop.js";
import { formatResult } from "../tools.js";
const args = {
    request: AddUsersToTeamRequest$zodSchema,
};
export const tool$teamsAddUsersToTeam = {
    name: "teams-add-users-to-team",
    description: `Add users to team

Add one or more users to a team with specified roles.<br><br>
<b>Behavior:</b><br>
<ul>
<li>Users already in the team are skipped</li>
<li>Default role is "member" if not specified</li>
<li>Sends invitation notification to added users</li>
</ul>
<b>Validation:</b><br>
<ul>
<li>All user IDs must be valid and from the same organization</li>
<li>Role must be one of the allowed values</li>
<li>Only team owner/admin can add members</li>
</ul>
`,
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await teamsAddUsersToTeam(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=teamsAddUsersToTeam.js.map