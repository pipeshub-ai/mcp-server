/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { semanticSearchSearchHistory } from "../../funcs/semanticSearchSearchHistory.js";
import { SearchHistoryRequest$zodSchema } from "../../models/searchhistoryop.js";
import { formatResult } from "../tools.js";
const args = {
    request: SearchHistoryRequest$zodSchema.optional(),
};
export const tool$semanticSearchSearchHistory = {
    name: "semantic-search-search-history",
    description: `Get search history

Retrieve your search history with pagination.<br><br>
<b>Overview:</b><br>
Returns a list of all searches performed by the authenticated user.
Each entry includes the original query, results, and metadata.<br><br>
<b>Pagination:</b><br>
Use <code>page</code> and <code>limit</code> to navigate through results.
`,
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await semanticSearchSearchHistory(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=semanticSearchSearchHistory.js.map