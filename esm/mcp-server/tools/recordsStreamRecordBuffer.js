/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { recordsStreamRecordBuffer } from "../../funcs/recordsStreamRecordBuffer.js";
import { StreamRecordBufferRequest$zodSchema } from "../../models/streamrecordbufferop.js";
import { formatResult } from "../tools.js";
const args = {
    request: StreamRecordBufferRequest$zodSchema,
};
export const tool$recordsStreamRecordBuffer = {
    name: "records-stream-record-buffer",
    description: `Stream record content

Stream the binary content of a record's file.<br><br>
<b>Overview:</b><br>
Returns the raw file content with appropriate Content-Type and Content-Disposition headers for download or inline viewing.<br><br>
<b>Use Cases:</b><br>
<ul>
<li>File downloads</li>
<li>Inline document preview</li>
<li>Content extraction pipelines</li>
</ul>
<b>Format Conversion:</b><br>
Use <code>convertTo</code> parameter to convert between formats (e.g., DOCX to PDF).
`,
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await recordsStreamRecordBuffer(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=recordsStreamRecordBuffer.js.map