/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { compactMap } from "../lib/primitives.js";
import { pathToFunc } from "../lib/url.js";
import { QueryServiceHealthOpServerList } from "../models/queryservicehealthop.js";
import { APIPromise } from "../types/async.js";
/**
 * [Query Service] Health check
 *
 * @remarks
 * ⚠️ <b>INTERNAL SERVICE ENDPOINT</b><br><br>
 *
 * Health check endpoint for the Query Service (Python FastAPI).<br><br>
 *
 * <b>Service:</b> Query Service<br>
 * <b>Port:</b> 8000<br>
 * <b>Base URL:</b> <code>http://localhost:8000</code><br><br>
 *
 * <b>Authentication:</b> None required for health check<br><br>
 *
 * <b>Note:</b> This is an internal service health endpoint. It also verifies
 * connectivity to the Connector Service as a dependency check.
 */
export function queryServiceHealth(client$, options) {
    return new APIPromise($do(client$, options));
}
async function $do(client$, options) {
    const baseURL$ = options?.serverURL
        || pathToFunc(QueryServiceHealthOpServerList[0], {
            charEncoding: "percent",
        })();
    const path$ = pathToFunc("/query/health")();
    const headers$ = new Headers(compactMap({
        Accept: "application/json",
    }));
    const context = {
        options: client$._options,
        baseURL: baseURL$ ?? "",
        operationID: "queryServiceHealth",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        method: "GET",
        baseURL: baseURL$,
        path: path$,
        headers: headers$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    return [doResult, {
            status: "complete",
            "request": req$,
            response: doResult.value,
        }];
}
//# sourceMappingURL=queryServiceHealth.js.map