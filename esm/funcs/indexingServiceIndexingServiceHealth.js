/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { compactMap } from "../lib/primitives.js";
import { pathToFunc } from "../lib/url.js";
import { IndexingServiceHealthOpServerList } from "../models/indexingservicehealthop.js";
import { APIPromise } from "../types/async.js";
/**
 * [Indexing Service] Health check
 *
 * @remarks
 * ⚠️ <b>INTERNAL SERVICE ENDPOINT</b><br><br>
 *
 * Health check endpoint for the Indexing Service (Python FastAPI).<br><br>
 *
 * <b>Service:</b> Indexing Service<br>
 * <b>Port:</b> 8091<br>
 * <b>Base URL:</b> <code>http://localhost:8091</code><br><br>
 *
 * <b>Authentication:</b> None required for health check<br><br>
 *
 * <b>Note:</b> This is an internal service. It processes Kafka messages
 * for document indexing and does not expose user-facing endpoints.
 * Health check also verifies Connector Service connectivity.
 */
export function indexingServiceIndexingServiceHealth(client$, options) {
    return new APIPromise($do(client$, options));
}
async function $do(client$, options) {
    const baseURL$ = options?.serverURL
        || pathToFunc(IndexingServiceHealthOpServerList[0], {
            charEncoding: "percent",
        })();
    const path$ = pathToFunc("/indexing/health")();
    const headers$ = new Headers(compactMap({
        Accept: "application/json",
    }));
    const context = {
        options: client$._options,
        baseURL: baseURL$ ?? "",
        operationID: "indexingServiceHealth",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        method: "GET",
        baseURL: baseURL$,
        path: path$,
        headers: headers$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    return [doResult, {
            status: "complete",
            "request": req$,
            response: doResult.value,
        }];
}
//# sourceMappingURL=indexingServiceIndexingServiceHealth.js.map