/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { compactMap } from "../lib/primitives.js";
import { pathToFunc } from "../lib/url.js";
import { OpenidConfigurationOpServerList } from "../models/openidconfigurationop.js";
import { APIPromise } from "../types/async.js";
/**
 * OpenID Connect Discovery
 *
 * @remarks
 * OpenID Connect Discovery Endpoint (RFC 8414).
 * <br><br>
 * Returns metadata about the OAuth/OIDC authorization server including
 * endpoint URLs, supported features, and capabilities.
 * <br><br>
 * <b>Use Cases:</b><br>
 * - Automatic client configuration<br>
 * - Discovering supported features<br>
 * - Getting endpoint URLs without hardcoding
 * <br><br>
 * <b>Note:</b> This endpoint does not require authentication.
 */
export function openIDConnectOpenidConfiguration(client$, options) {
    return new APIPromise($do(client$, options));
}
async function $do(client$, options) {
    const baseURL$ = options?.serverURL
        || pathToFunc(OpenidConfigurationOpServerList[0], {
            charEncoding: "percent",
        })();
    const path$ = pathToFunc("/.well-known/openid-configuration")();
    const headers$ = new Headers(compactMap({
        Accept: "application/json",
    }));
    const context = {
        options: client$._options,
        baseURL: baseURL$ ?? "",
        operationID: "openidConfiguration",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        method: "GET",
        baseURL: baseURL$,
        path: path$,
        headers: headers$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    return [doResult, {
            status: "complete",
            "request": req$,
            response: doResult.value,
        }];
}
//# sourceMappingURL=openIDConnectOpenidConfiguration.js.map