/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { encodeSimple } from "../lib/encodings.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { resolveSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { ConnectorInternalStreamRecordOpServerList, ConnectorInternalStreamRecordRequest$zodSchema, } from "../models/connectorinternalstreamrecordop.js";
import { APIPromise } from "../types/async.js";
/**
 * [Connector Service] Internal stream record
 *
 * @remarks
 * ⚠️ <b>INTERNAL SERVICE ENDPOINT</b><br><br>
 *
 * Internal endpoint for streaming record content (service-to-service).<br><br>
 *
 * <b>Service:</b> Connector Service<br>
 * <b>Port:</b> 8088<br>
 * <b>Base URL:</b> <code>http://localhost:8088</code><br><br>
 *
 * <b>Authentication:</b> Requires scoped service token<br><br>
 *
 * <b>Use Case:</b> Used by Indexing Service to fetch file content for processing.
 */
export function connectorServiceConnectorInternalStreamRecord(client$, security, request, options) {
    return new APIPromise($do(client$, security, request, options));
}
async function $do(client$, security, request, options) {
    const parsed$ = safeParse(request, (value$) => ConnectorInternalStreamRecordRequest$zodSchema.parse(value$), "Input validation failed");
    if (!parsed$.ok) {
        return [parsed$, { status: "invalid" }];
    }
    const payload$ = parsed$.value;
    const body$ = null;
    const baseURL$ = options?.serverURL
        || pathToFunc(ConnectorInternalStreamRecordOpServerList[0], {
            charEncoding: "percent",
        })();
    const pathParams$ = {
        record_id: encodeSimple("record_id", payload$.record_id, {
            explode: false,
            charEncoding: "percent",
        }),
    };
    const path$ = pathToFunc("/connector/api/v1/internal/stream/record/{record_id}")(pathParams$);
    const headers$ = new Headers(compactMap({
        Accept: "application/octet-stream",
    }));
    const requestSecurity = resolveSecurity([
        {
            fieldName: "Authorization",
            type: "http:bearer",
            value: security?.scopedToken,
        },
    ]);
    const context = {
        options: client$._options,
        baseURL: baseURL$ ?? "",
        operationID: "connectorInternalStreamRecord",
        oAuth2Scopes: null,
        resolvedSecurity: requestSecurity,
        securitySource: security,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        security: requestSecurity,
        method: "GET",
        baseURL: baseURL$,
        path: path$,
        headers: headers$,
        body: body$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    return [doResult, {
            status: "complete",
            "request": req$,
            response: doResult.value,
        }];
}
//# sourceMappingURL=connectorServiceConnectorInternalStreamRecord.js.map