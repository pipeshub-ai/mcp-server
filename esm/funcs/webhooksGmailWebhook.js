/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { compactMap } from "../lib/primitives.js";
import { pathToFunc } from "../lib/url.js";
import { GmailWebhookOpServerList } from "../models/gmailwebhookop.js";
import { APIPromise } from "../types/async.js";
/**
 * [Connector Service] Gmail webhook
 *
 * @remarks
 * ⚠️ <b>INTERNAL SERVICE ENDPOINT</b><br><br>
 *
 * Webhook endpoint for Gmail Pub/Sub notifications.<br><br>
 *
 * <b>Service:</b> Connector Service<br>
 * <b>Port:</b> 8088<br>
 * <b>Base URL:</b> <code>http://localhost:8088</code><br><br>
 *
 * <b>Authentication:</b> None required - uses Google Pub/Sub verification<br><br>
 *
 * <b>Note:</b> This endpoint receives notifications when new emails arrive
 * or email labels change.
 */
export function webhooksGmailWebhook(client$, options) {
    return new APIPromise($do(client$, options));
}
async function $do(client$, options) {
    const baseURL$ = options?.serverURL
        || pathToFunc(GmailWebhookOpServerList[0], { charEncoding: "percent" })();
    const path$ = pathToFunc("/connector/gmail/webhook")();
    const headers$ = new Headers(compactMap({
        Accept: "*/*",
    }));
    const context = {
        options: client$._options,
        baseURL: baseURL$ ?? "",
        operationID: "gmailWebhook",
        oAuth2Scopes: null,
        resolvedSecurity: null,
        securitySource: null,
        retryConfig: options?.retries
            || client$._options.retryConfig
            || { strategy: "none" },
        retryCodes: options?.retryCodes || [
            "429",
            "500",
            "502",
            "503",
            "504",
        ],
    };
    const requestRes = client$._createRequest(context, {
        method: "POST",
        baseURL: baseURL$,
        path: path$,
        headers: headers$,
        userAgent: client$._options.userAgent,
        timeoutMs: options?.timeoutMs || client$._options.timeoutMs
            || -1,
    }, options);
    if (!requestRes.ok) {
        return [requestRes, { status: "invalid" }];
    }
    const req$ = requestRes.value;
    const doResult = await client$._do(req$, {
        context,
        errorCodes: [],
        retryConfig: context.retryConfig,
        retryCodes: context.retryCodes,
    });
    if (!doResult.ok) {
        return [doResult, { status: "request-error", request: req$ }];
    }
    return [doResult, {
            status: "complete",
            "request": req$,
            response: doResult.value,
        }];
}
//# sourceMappingURL=webhooksGmailWebhook.js.map